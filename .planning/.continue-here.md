# Continue Here — watchdirs

**Paused:** 2026-06-14 (context exhaustion at 100%)
**Milestone:** v1.0 — 3 of 4 phases complete (Phase 3 complete + verified)

## Current Position

- Phase 3 (Pressure Gap Diagnostics): **complete, verified, code-review clean** (169 tests pass).
- Mid-`/gsd-discuss-phase 4` when context ran out. Discussion produced decisions but **no CONTEXT.md written yet**.
- User decided to **insert a new Phase 3.5 "Storage Efficiency" before Phase 4** — NOT yet recorded in ROADMAP.md/REQUIREMENTS.md.

## What's Done

- Phases 1–3 shipped. Phase 3 added `df-vs-index`, `deleted-open-files`, `docker-enrichment`, and `report` diagnostic_hints/pressure_summary. Verified live on real data.
- Two memory files written (full detail): `phase-35-storage-efficiency.md`, `phase-4-decisions.md`, indexed in MEMORY.md.

## Remaining Work (next session, in order)

1. **Record Phase 3.5 in the roadmap** — `/gsd-phase` to insert "3.5 Storage Efficiency" before Phase 4; add requirements (suggest STOR-01 path dictionary, STOR-02 size+duration benchmark, STOR-03 collect progress/ETA logging) to REQUIREMENTS.md.
2. **Plan + execute Phase 3.5** — `/gsd-discuss-phase 3.5` or `/gsd-plan-phase 3.5`.
3. **Resume Phase 4 discuss/plan** AFTER 3.5 (timer interval depends on 3.5's measured scan duration).

## Key Decisions (detail in memory files)

- **Phase 3.5 (storage):** path dictionary INSIDE SQLite first (each unique path stored once, `directory_sizes` references `path_id`); engine change to DuckDB/SurrealDB only if dictionary insufficient. Mandatory benchmark of REAL DB size (before/after) and REAL full-scan duration. `collect` must log progress + ETA + final duration.
- **Phase 4 (scheduling):** run scheduled collect as **root** (system unit + hardening); DB at **/srv/watchdirs/**; snapshots "Способ Б" (one hourly collect + age-based thinning: <14d keep all, 14–90d keep 1/day, >90d delete); systemd timers, flock lock, nice+idle I/O, weekly VACUUM after prune; scan roots `/ /var /opt /srv /home`.

## Critical Anti-Patterns

| Anti-pattern | Severity | Note |
|---|---|---|
| Running scheduled collect / filling a prod DB before the path dictionary lands | blocking | User's explicit instruction: storage must be deduplicated first, or the tool bloats the disk it's meant to protect. Do Phase 3.5 before any Phase 4 scheduling. |
| Eyeballing DB size/scan duration instead of measuring | warning | User pushed back on unmeasured ~8GB estimate; Phase 3.5 must produce real measurements before choosing the timer interval. |

## Resume

`/gsd-progress` to reorient, then start with Phase 3.5 (`/gsd-phase` to insert it). Memory loads automatically and carries the full rationale.
