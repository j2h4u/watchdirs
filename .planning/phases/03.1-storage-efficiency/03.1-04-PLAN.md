---
phase: 03.1-storage-efficiency
plan: 04
type: tdd
wave: 2
depends_on:
  - 03.1-01
  - 03.1-02
files_modified:
  - src/watchdirs/bench/size.py
  - src/watchdirs/bench/duration.py
  - tests/bench/test_size.py
autonomous: false
user_setup:
  - service: localhost sudo (cold-cache benchmark only)
    why: "/proc/sys/vm/drop_caches is root-only (mode 0200) on this host; required for the cold-cache scan timing leg of D-10"
    dashboard_config:
      - task: "Operator runs the cold-cache duration leg via `sudo` (member of sudo group), OR the harness falls back to posix_fadvise(DONTNEED), OR reports 'warm only' — never silently mislabels cold as warm"
        location: "manual benchmark run, NOT the unattended unit suite"
requirements:
  - size-harness
  - byte-budget
  - duration
must_haves:
  truths:
    - "The harness replicates one real scan into ~412 synthetic snapshots under BOTH old-BLOB and new-dictionary schemas with IDENTICAL PRAGMAs, VACUUMs both, and compares file sizes"
    - "Size is measured correctly: page_count*page_size reconciles with stat() bytes post-VACUUM, with table-vs-index broken out via dbstat (including the UNIQUE autoindex cost)"
    - "The D-09 PASS/FAIL gate is a per-snapshot byte budget set against the MEASURED after-VACUUM size and the measured churn from Plan 01 — not an idealized model"
    - "Scan wall-clock is reported cold-cache vs warm under nice/ionice -c2 -n7, median of >=3 runs"
  artifacts:
    - path: "src/watchdirs/bench/size.py"
      provides: "replicate-VACUUM-compare size harness + dbstat attribution + D-09 gate evaluation"
      min_lines: 60
    - path: "src/watchdirs/bench/duration.py"
      provides: "cold/warm scan wall-clock under nice/ionice, median-of->=3, sudo-or-skip cold"
      min_lines: 40
    - path: "tests/bench/test_size.py"
      provides: "OLD-vs-NEW identical-PRAGMA build + page_count*page_size == stat() reconciliation"
      contains: "def test_"
  key_links:
    - from: "src/watchdirs/bench/size.py"
      to: "src/watchdirs/db/schema.sql (paths + directory_sizes new schema) + db/connection.py PRAGMAs"
      via: "builds the NEW-side DB via initialize_database/insert_directory_rows; OLD-side via an inline legacy DDL string local to the harness"
      pattern: "dbstat|VACUUM|page_count"
    - from: "src/watchdirs/bench/size.py"
      to: "src/watchdirs/bench/churn.py (measured churn)"
      via: "churn rate drives synthetic-snapshot replication and sets the D-09 budget"
      pattern: "churn|dedup"
---

<objective>
Build the Hybrid-A size benchmark (D-08 method A, D-10) and the cold/warm duration harness,
then evaluate the D-09 PASS/FAIL per-snapshot byte-budget gate against the MEASURED after-
VACUUM size and the measured churn from Plan 01. This is the deliverable that proves the
phase worth shipping (or, on a wide miss, triggers the D-07 DuckDB-escalation conversation).

Purpose: gate Phase 4 scheduling on a real measured size win, not an eyeballed estimate.
Output: `src/watchdirs/bench/size.py` (replicate→VACUUM→dbstat compare + gate),
`src/watchdirs/bench/duration.py` (nice/ionice cold/warm median), and `tests/bench/test_size.py`.

NOTE: `autonomous: false` — the cold-cache duration leg and the final gate evaluation against
REAL churn require an operator-run benchmark (sudo for drop_caches; 3–5 spaced real scans
from Plan 01). The harness code + its unit test are fully automated; the human checkpoint
gates the gate-number sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-storage-efficiency/03.1-CONTEXT.md
@.planning/phases/03.1-storage-efficiency/03.1-RESEARCH.md
@.planning/phases/03.1-storage-efficiency/03.1-PATTERNS.md
@.planning/phases/03.1-storage-efficiency/03.1-VALIDATION.md
@.planning/phases/03.1-storage-efficiency/03.1-01-SUMMARY.md
@.planning/phases/03.1-storage-efficiency/03.1-02-SUMMARY.md
</context>

## Artifacts this phase produces

NEW symbols introduced by this plan (exclude from drift verification):
- `src/watchdirs/bench/size.py`: replicate-synthetic-snapshots, VACUUM, dbstat per-object
  attribution (Pattern 3), `page_count*page_size == stat()` reconciliation, OLD-vs-NEW
  compare, and a D-09 gate evaluator (`evaluate_byte_budget` or similar) returning PASS/FAIL
  + the reduction ratio as color.
- `src/watchdirs/bench/duration.py`: cold/warm scan wall-clock under `nice`/`ionice -c2 -n7`,
  median-of-≥3, with `sudo drop_caches` OR `posix_fadvise(DONTNEED)` OR "warm only" fallback.
- An inline OLD-BLOB legacy DDL string LOCAL to `size.py` (the harness needs to build the
  pre-rewrite schema to compare against; this is benchmark scaffolding, NOT a compat shim in
  the product — the product schema has no old path).
- `tests/bench/test_size.py`.

Depends on Plan 02's `paths`/`path_id` schema + connection PRAGMAs (NEW-side DB) and Plan
01's churn measurement (drives replication / sets the budget).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Wave-0 test for the size harness (identical PRAGMAs + page_count*page_size == stat() reconciliation)</name>
  <files>tests/bench/test_size.py</files>
  <read_first>
    - .planning/phases/03.1-storage-efficiency/03.1-RESEARCH.md (Pattern 3 dbstat `SELECT name, SUM(pgsize), SUM(ncell) FROM dbstat GROUP BY name`; Pattern 4 PRAGMA ordering; Pitfall 4 mismatched-PRAGMA invalidation; Pitfall 6 the UNIQUE autoindex doubles path storage)
    - .planning/phases/03.1-storage-efficiency/03.1-VALIDATION.md (Wave 0: `tests/bench/test_size.py` — OLD vs NEW identical PRAGMAs + reconciliation)
    - .planning/phases/03.1-storage-efficiency/03.1-PATTERNS.md (`bench/size.py` section: reuse initialize_database + open_connection PRAGMA setup, identical PRAGMAs on both DBs, VACUUM then dbstat, assert page_count*page_size == stat(), count sqlite_autoindex_paths_1)
    - tests/test_db_schema.py (PRAGMA/version assertion idiom)
    - tests/test_reporting_queries.py (`_open_db` fixture idiom)
  </read_first>
  <behavior>
    - RED: `tests/bench/test_size.py` builds a small OLD-BLOB DB and a small NEW-DICT DB via
      the harness (same row data, ~3 synthetic snapshots) and asserts: (a) both DBs were
      built with IDENTICAL `page_size`/`journal_mode`/`auto_vacuum` PRAGMAs; (b) after
      VACUUM, `page_count*page_size == os.path.getsize()` for EACH DB (reconciliation); (c)
      the dbstat per-object breakdown for the NEW DB includes a `sqlite_autoindex_paths_1`
      entry counted in the total (Pitfall 6); (d) the NEW DB total is smaller than the OLD
      DB total for a low-churn fixture (the win exists, directionally).
    - RED: a gate-evaluator test — given a measured after-VACUUM per-snapshot byte figure and
      a budget, `evaluate_byte_budget` returns PASS when under budget and FAIL when over, and
      reports the reduction ratio as color (not as the gate).
    - RED until Task 2 implements `size.py`.
  </behavior>
  <action>
    Create `tests/bench/test_size.py`. Use a small deterministic fixture (a few hundred rows,
    ~3 synthetic snapshots, a fixed low churn) so the test runs in the unit-suite latency
    budget — the FULL ~412-snapshot benchmark is the operator's manual run, not the suite.
    Assert identical PRAGMAs on both DBs, post-VACUUM `page_count*page_size == stat()` for
    each, presence of `sqlite_autoindex_paths_1` in the NEW dbstat breakdown, and NEW < OLD
    total. Add a pure-function test of the D-09 gate evaluator (PASS under budget, FAIL over,
    ratio reported separately). RED until `size.py` exists.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/bench/test_size.py -x 2>&1 | grep -E "ModuleNotFoundError|ImportError|AttributeError|error|assert" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bench/test_size.py` exists and FAILS (RED) before Task 2.
    - Asserts identical PRAGMAs on OLD and NEW DBs (Pitfall 4 guard).
    - Asserts `page_count*page_size == os.path.getsize()` post-VACUUM for each DB.
    - Asserts `sqlite_autoindex_paths_1` appears in the NEW dbstat breakdown (Pitfall 6).
    - Includes a PASS/FAIL gate-evaluator unit test with the ratio reported as color, not gated on.
  </acceptance_criteria>
  <done>Failing size-harness test pins identical-PRAGMA build + reconciliation + autoindex accounting + gate semantics; commit `test(03.1-04): add failing size-harness + byte-budget gate test`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement size.py (replicate → VACUUM → dbstat compare → D-09 gate) (GREEN)</name>
  <files>src/watchdirs/bench/size.py</files>
  <read_first>
    - .planning/phases/03.1-storage-efficiency/03.1-RESEARCH.md (Pattern 3 dbstat SQL; Pattern 4 virgin-conn PRAGMAs; Don't Hand-Roll: dbstat for attribution, page_count*page_size AND stat() post-VACUUM; Pitfall 6 autoindex)
    - .planning/phases/03.1-storage-efficiency/03.1-PATTERNS.md (`bench/size.py` No-Analog row + the replicate-VACUUM-compare flow; reuse `insert_directory_rows` batching + `open_connection` PRAGMA setup)
    - .planning/phases/03.1-storage-efficiency/03.1-CONTEXT.md (D-08 method A spine; D-09 per-snapshot byte budget gate; D-10 measure-size-correctly rules)
    - src/watchdirs/db/migrations.py (initialize_database, insert_directory_rows, INSERT_BATCH_SIZE — reuse for the NEW-side build)
    - src/watchdirs/db/connection.py (open_connection PRAGMAs — apply identically to BOTH benchmark DBs)
    - src/watchdirs/bench/churn.py (Plan 01: measured churn feeds replication / budget)
  </read_first>
  <behavior>
    - GREEN: implement the harness so Task 1's test passes. A `replicate_snapshots` builds N
      synthetic snapshots from one real scan's rows under a target schema, driven by a churn
      rate (reusing Plan 01's measurement). A `measure_db_size` returns total bytes via
      `page_count*page_size`, the `stat()` bytes, and the per-object dbstat breakdown (table,
      user index, `sqlite_autoindex_paths_1`, sqlite_schema), and asserts the two totals
      reconcile post-VACUUM. A `compare_old_vs_new` builds both DBs with IDENTICAL PRAGMAs,
      VACUUMs both, and returns the size delta + per-snapshot byte figure. `evaluate_byte_budget`
      applies the D-09 gate (PASS/FAIL against the measured per-snapshot budget) and reports
      the reduction ratio as color.
    - Runnable via `uv run python -m watchdirs.bench.size <real_scan_db> --snapshots 412 --churn <measured>`.
  </behavior>
  <action>
    Implement `src/watchdirs/bench/size.py`. For the NEW-side DB, reuse `initialize_database`
    + `open_connection` (identical PRAGMAs) and `insert_directory_rows` batching. For the
    OLD-side DB, define an inline legacy-DDL constant LOCAL to the harness (the pre-rewrite
    `directory_sizes` with path/parent_path/name BLOB + the old indexes) and a matching
    inline insert — this is benchmark scaffolding to measure the baseline, explicitly NOT a
    product compat shim (the product schema has none). Apply the SAME `page_size`/
    `journal_mode`/`auto_vacuum` to both. `VACUUM` both before measuring. Use the dbstat SQL
    from Pattern 3 for per-object attribution; assert `page_count*page_size ==
    os.path.getsize()` post-VACUUM (Don't-Hand-Roll). Count `sqlite_autoindex_paths_1` in the
    NEW total (Pitfall 6) and set the per-snapshot byte figure against THAT measured number.
    `evaluate_byte_budget(per_snapshot_bytes, budget_bytes)` returns PASS/FAIL; compute and
    report the reduction ratio separately as color (D-09). Make the module a runnable entry
    point printing the OLD/NEW breakdown, the per-snapshot byte figure, the ratio, and the
    PASS/FAIL verdict to stdout (dev tool; stdout fine here). Drive replication with the
    churn measured by Plan 01.
  </action>
  <verify>
    <automated>uv run --with pytest pytest tests/bench/test_size.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --with pytest pytest tests/bench/test_size.py -x` PASSES (GREEN).
    - `size.py` builds OLD and NEW DBs with identical PRAGMAs, VACUUMs both, and reconciles `page_count*page_size == stat()`.
    - dbstat per-object attribution includes `sqlite_autoindex_paths_1` and it is counted in the NEW total (Pitfall 6).
    - `evaluate_byte_budget` returns PASS/FAIL on the measured per-snapshot byte figure; the reduction ratio is reported as color, not the gate.
    - Replication is driven by a churn rate sourced from `watchdirs.bench.churn`.
  </acceptance_criteria>
  <done>Size harness measures the real win and evaluates the D-09 gate; commit `feat(03.1-04): size benchmark harness + D-09 byte-budget gate`.</done>
</task>

<task type="execute">
  <name>Task 3: Implement duration.py (cold/warm scan wall-clock, nice/ionice, median-of->=3, sudo-or-skip cold)</name>
  <files>src/watchdirs/bench/duration.py</files>
  <read_first>
    - .planning/phases/03.1-storage-efficiency/03.1-RESEARCH.md (Don't-Hand-Roll: drop_caches gating, time.monotonic; Pitfall 5 drop_caches needs root mode 0200 → sudo OR posix_fadvise(DONTNEED) OR "warm only"; Environment Availability: nice/ionice present at /usr/bin, `-c2 -n7` works)
    - .planning/phases/03.1-storage-efficiency/03.1-PATTERNS.md (`bench/duration.py` No-Analog row: wrap scan_root under nice/ionice -c2 -n7, median of >=3, cold vs warm)
    - .planning/phases/03.1-storage-efficiency/03.1-CONTEXT.md (D-10 scan wall-clock: cold-cache vs warm reported separately, prod nice/ionice -c2 -n7, median of >=3 + spread, root-by-root)
    - src/watchdirs/cli.py (~242 the `scan_root` invocation idiom to mirror in the subprocess wrap)
  </read_first>
  <action>
    Implement `src/watchdirs/bench/duration.py`. Time a scan wall-clock with `time.monotonic()`
    (NOT time.time — clock-step immune). Run cold-cache and warm-cache legs SEPARATELY,
    root-by-root, median of ≥3 runs plus the spread. Wrap each scan under `nice` and
    `ionice -c2 -n7` (both at /usr/bin). For the cold leg, attempt `sudo sh -c 'echo 3 >
    /proc/sys/vm/drop_caches'`; if sudo is unavailable, fall back to opening the scanned
    target/DB fd and `os.posix_fadvise(fd, 0, 0, os.POSIX_FADV_DONTNEED)`; if NEITHER works,
    report the cold leg as "cold unavailable, warm only" rather than silently mislabeling it
    cold (Pitfall 5). Report cold and warm medians + spread separately. Runnable via
    `uv run python -m watchdirs.bench.duration`. This module is operator-run (the cold leg is
    privileged and the real scan is slow) — it is NOT part of the unattended unit suite (see
    the human-verify checkpoint below and VALIDATION.md Manual-Only Verifications).
  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; ast.parse(open('src/watchdirs/bench/duration.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/watchdirs/bench/duration.py` parses and is importable.
    - Uses `time.monotonic()` (grep present); does NOT use `time.time()` for elapsed (grep absent in the timing path).
    - Wraps scans under `nice` and `ionice -c2 -n7`.
    - Cold leg attempts sudo drop_caches, falls back to `posix_fadvise(POSIX_FADV_DONTNEED)`, and reports "warm only" if neither — never mislabels cold (grep for the three branches).
    - Reports cold and warm medians + spread separately, root-by-root, median of ≥3.
  </acceptance_criteria>
  <done>Duration harness measures cold/warm scan timing under prod priority with the privilege fallback; commit `feat(03.1-04): cold/warm scan duration harness`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator gate — set the D-09 byte budget against measured churn and confirm PASS</name>
  <files>.planning/phases/03.1-storage-efficiency/03.1-04-SUMMARY.md</files>
  <what-built>
    The full benchmark harness (size + duration) and the D-09 gate evaluator. The unit-suite
    portion (small fixtures) is automated and green. The REAL gate decision requires operator
    inputs that cannot run unattended: (1) the measured churn series from Plan 01's 3–5 spaced
    real scans, (2) the cold-cache duration leg (needs sudo for drop_caches).
  </what-built>
  <how-to-verify>
    1. Run the churn driver from Plan 01 on the dev DB produced by your 3–5 spaced `collect`
       runs; record the measured churn rate + dedup_ratio.
    2. Run the size benchmark against a real scan, replicating ~412 synthetic snapshots at the
       MEASURED churn:
       `uv run python -m watchdirs.bench.size <real_scan_db> --snapshots 412 --churn <measured>`
       Confirm it prints OLD/NEW dbstat breakdown (incl. sqlite_autoindex_paths_1),
       per-snapshot bytes, reduction ratio, and PASS/FAIL.
    3. Set the D-09 per-snapshot byte budget against the MEASURED after-VACUUM size (not an
       idealized model) and confirm the verdict is PASS. If it FAILS by a wide margin, that
       is the D-07 DuckDB-escalation trigger — stop and discuss, do not silently proceed.
    4. Run the cold/warm duration leg under sudo (or note the fallback used):
       `sudo --preserve-env uv run python -m watchdirs.bench.duration` (or the documented
       posix_fadvise/"warm only" path). Confirm cold and warm medians are reported separately.
  </how-to-verify>
  <action>
    BLOCKING human checkpoint — do not auto-advance (this is a forensic-integrity gate, never
    auto-approvable). Pause and present the four `<how-to-verify>` steps to the operator:
    measure real churn (Plan 01 driver), run the ~412-snapshot size benchmark at the measured
    churn, set the D-09 per-snapshot byte budget against the MEASURED after-VACUUM size, and
    run the cold/warm duration leg under sudo (or the documented fallback). Record the chosen
    budget number, the reduction ratio (as color), and the PASS/FAIL verdict in the SUMMARY.
    On a wide miss, STOP and surface the D-07 DuckDB-escalation decision rather than proceeding.
  </action>
  <verify>
    <human-check>Operator confirms: D-09 budget set against measured after-VACUUM size, size benchmark verdict is PASS (or D-07 escalation raised), and cold/warm duration medians reported separately.</human-check>
  </verify>
  <resume-signal>Type "approved" with the chosen D-09 budget number and the PASS verdict, or "escalate" if the gate missed by a wide margin (D-07).</resume-signal>
  <done>The D-09 per-snapshot byte budget is set against measured data, the size benchmark PASSES (or a D-07 escalation is explicitly raised), and cold/warm scan durations are recorded in the SUMMARY — Phase 4 scheduling is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark harness → privileged drop_caches | The ONLY privileged operation in the phase; runs in the dev/benchmark context, never in collect runtime. |
| real scan rows → throwaway benchmark DBs | The harness writes synthetic snapshots to throwaway DBs, not a prod store (no prod store exists yet — gated behind this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04-01 | Elevation of Privilege | cold-cache `sudo drop_caches` | mitigate | Privileged op gated behind explicit operator `sudo`, run ONLY from the benchmark (never collect runtime); unprivileged `posix_fadvise` fallback; "warm only" if neither. Confined to `duration.py`. |
| T-03.1-04-02 | Tampering / measurement integrity | OLD-vs-NEW size comparison | mitigate | Identical PRAGMAs on both DBs + post-VACUUM `page_count*page_size == stat()` reconciliation (Pitfall 4); autoindex counted (Pitfall 6) so the gate is set against real measured bytes, not an inflated/idealized ratio. |
| T-03.1-04-03 | Tampering | benchmark synthetic-snapshot inserts | mitigate | Path bytes bound as parameters in both OLD and NEW inline inserts; no interpolation (ASVS V5). |
| T-03.1-04-04 | Information disclosure | benchmark stdout | accept | Dev tool, local operator, prints size/timing figures and path counts; no network surface. Proportional to offline CLI. |
</threat_model>

<verification>
- `uv run --with pytest pytest tests/bench/test_size.py -x` green; `duration.py` parses/imports.
- `uv run --with pytest pytest -q` (full suite) still green (this plan adds bench files only).
- Phase gate (operator, via the human-verify checkpoint): size benchmark PASS at the measured
  per-snapshot byte budget (D-09); cold/warm duration reported separately. A wide miss triggers
  the D-07 DuckDB-escalation discussion (do not silently proceed).
</verification>

<success_criteria>
- Size harness replicates under identical PRAGMAs, VACUUMs, and reconciles size correctly with table-vs-index (incl. autoindex) attribution.
- D-09 per-snapshot byte budget gate evaluated against MEASURED after-VACUUM size + measured churn; PASS confirmed (or D-07 escalation surfaced).
- Cold/warm scan duration measured under nice/ionice with the privilege fallback.
</success_criteria>

<output>
Create `.planning/phases/03.1-storage-efficiency/03.1-04-SUMMARY.md` when done.
</output>
