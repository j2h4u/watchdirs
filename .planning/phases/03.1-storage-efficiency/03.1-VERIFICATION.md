---
phase: 03.1-storage-efficiency
verified: 2026-06-15T00:00:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
---

# Phase 03.1: Storage Efficiency Verification Report

**Phase Goal:** Replace per-row blob path/name columns with a flat path-dictionary (int path_id FKs), prove a real MEASURED per-snapshot size win (D-09 gate) sufficient to NOT escalate to DuckDB (D-07), wire reporting/diagnostics onto the new schema with proven output equivalence, deliver size + cold/warm duration benchmark harnesses, and add collect observability (stderr progress/summary, pure-JSON stdout). Derived from locked decisions D-01..D-11.
**Verified:** 2026-06-15
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Must-haves merged from the phase goal + the `must_haves` frontmatter of all 5 PLANs (ROADMAP §03.1 declares no separate Success Criteria array, so the goal text + PLAN frontmatter are the contract).

| #  | Truth (source plan)                                                                                          | Status     | Evidence |
| -- | ------------------------------------------------------------------------------------------------------------ | ---------- | -------- |
| 1  | A dev/CI tool measures per-snapshot path churn on the EXISTING schema before rewrite (P01)                    | ✓ VERIFIED | `bench/churn.py` `measure_churn`/`measure_churn_series`; `tests/bench/test_churn.py` in suite |
| 2  | dedup_ratio (total_rows/distinct_paths) is computable from real multi-snapshot data (P01)                     | ✓ VERIFIED | `measure_cardinality` → `dedup_ratio`; SUMMARY records measured churn=0 / sweep |
| 3  | Flat path dictionary stores each distinct path once; `directory_sizes` references int `path_id`/`parent_id` (P02) | ✓ VERIFIED | `schema.sql:15-31` — `paths(id, path TEXT UNIQUE)`, `path_id NOT NULL REFERENCES paths(id)`, `parent_id REFERENCES paths(id)` |
| 4  | Resolving an ALREADY-SEEN path returns its existing id (D-04/RETURNING regression impossible) (P02)           | ✓ VERIFIED | `migrations.py:102-117` `_resolve_path_id` = cache→SELECT-on-miss→INSERT, NO `OR IGNORE`/`RETURNING` (grep both = 0) |
| 5  | blob path/name/parent_path columns GONE; SCHEMA_VERSION=3 (P02)                                               | ✓ VERIFIED | grep `name BLOB`/`parent_path`/`path BLOB` in schema.sql = 0; `migrations.py:11` `SCHEMA_VERSION = 3` |
| 6  | Virgin-connection page_size/auto_vacuum(FULL)/application_id PRAGMAs (D-05) (P02)                             | ✓ VERIFIED | `connection.py:26-28` page_size=8192, auto_vacuum=FULL, application_id=0x57645273 on size==0 |
| 7  | diff/top/storage-domain reports return output identical to pre-rewrite (golden equivalence) (P03)            | ✓ VERIFIED | `tests/test_reporting_equivalence.py` (5085 B, 4 golden tests) passes |
| 8  | Cross-snapshot diff joins on integer path_id (int-equality), not BLOB equality (P03)                          | ✓ VERIFIED | `queries.py:415-451` `all_ids` CTE over path_id; `prev.path_id = a.path_id`; classification keys `path_id IS NULL` |
| 9  | docker/containerd path-hint GLOB resolves under the dictionary, binding byte prefixes (P03)                   | ✓ VERIFIED | `cli.py:1063-1064` `JOIN paths p ON p.id=ds.path_id ... p.path GLOB ?`; bound `prefix + b"/*"` |
| 10 | Python prefix/ancestor/mount-longest-prefix logic still receives full path bytes, unchanged (P03)            | ✓ VERIFIED | `queries.py:425,445` `JOIN paths p` selects `p.path AS path`; `COALESCE(cp.path,pp.path)` for parent; bytes consumers untouched |
| 11 | Harness replicates one scan into ~412 synthetic snapshots under OLD & NEW schemas, IDENTICAL PRAGMAs, VACUUMs both, compares sizes (P04) | ✓ VERIFIED | `size.py` `replicate_snapshots`, `_build_new_db`/`_build_old_db` both via `open_connection` (identical PRAGMAs), `compare_old_vs_new` |
| 12 | Size measured correctly: page_count*page_size reconciles stat() post-VACUUM, table-vs-index via dbstat incl. UNIQUE autoindex (P04) | ✓ VERIFIED | `size.py:247-276` `measure_db_size` VACUUM + `wal_checkpoint(TRUNCATE)` + `DBSTAT_SQL` per-object; reconciliation asserted in test |
| 13 | D-09 PASS/FAIL gate is a per-snapshot byte budget against MEASURED after-VACUUM size + measured churn (P04)    | ✓ VERIFIED | `evaluate_byte_budget`/`reduction_ratio`; smoke: `evaluate_byte_budget(40900,49152)→passed=True`; operator PASS recorded |
| 14 | Benchmarks are idempotent: size harness wipes scratch DBs (+ -wal/-shm) before each build (P04)               | ✓ VERIFIED | `size.py:288-301` `_remove_db_files` wipes `""/-wal/-shm`, called in both build paths; `test_rerun_in_same_workdir_is_idempotent` passes |
| 15 | collect emits INFO progress (dirs/rate/ETA, monotonic) + one structured end-summary to stderr (P05)           | ✓ VERIFIED | `cli.py` `compute_eta`/`log_progress`/`log_summary`; wired at lines 411 (per-root) & 463 (finally) |
| 16 | stdout stays pure JSON — observability never pollutes the JSON contract (P05)                                 | ✓ VERIFIED | `cli.py:92` `StreamHandler(sys.stderr)`; grep `StreamHandler(sys.stdout)`=0; `tests/test_collect_observability.py` passes |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/watchdirs/db/schema.sql` | paths dict + int directory_sizes | ✓ VERIFIED | paths table + path_id/parent_id FKs + int-keyed indexes; blob cols absent |
| `src/watchdirs/db/migrations.py` | _resolve_path_id, SCHEMA_VERSION 3 | ✓ VERIFIED | cache→SELECT→INSERT, no RETURNING; version 3 |
| `src/watchdirs/db/connection.py` | virgin PRAGMAs | ✓ VERIFIED | page_size/auto_vacuum/application_id |
| `src/watchdirs/reporting/queries.py` | 6× JOIN paths, int-equality diff | ✓ VERIFIED | 6 JOIN paths; all_ids CTE; int-equality |
| `src/watchdirs/cli.py` | docker GLOB + observability helpers | ✓ VERIFIED | GLOB via JOIN paths; stderr-only logging helpers |
| `src/watchdirs/bench/churn.py` | churn/cardinality measurement | ✓ VERIFIED | measure_churn(_series)/measure_cardinality |
| `src/watchdirs/bench/size.py` | replicate/VACUUM/dbstat + D-09 gate | ✓ VERIFIED | all 5 functions present; imports clean |
| `src/watchdirs/bench/duration.py` | cold/warm legs + fallback | ✓ VERIFIED | measure_root/cold/warm legs; 3-branch privilege fallback |
| `tests/test_reporting_equivalence.py` | golden equivalence | ✓ VERIFIED | 4 golden tests pass |
| `tests/test_collect_observability.py` | stdout-purity + stderr summary | ✓ VERIFIED | passes |
| `tests/bench/test_size.py` | measurement-integrity + idempotency | ✓ VERIFIED | incl. test_rerun_in_same_workdir_is_idempotent |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| scanner/insert_directory_rows | paths table | `_resolve_path_id` dedup cache | ✓ WIRED | `migrations.py:227,229` resolves path + parent_path to ints before executemany |
| reporting diff | directory_sizes/paths | int-equality join + JOIN paths | ✓ WIRED | `queries.py:444-453` |
| cli docker hint | paths | JOIN paths + GLOB | ✓ WIRED | `cli.py:1063-1064` |
| run_collect | logging→stderr | configure_collect_logging + log_progress/log_summary | ✓ WIRED | `cli.py:306,411,463` |
| size benchmark build | scratch DB | _remove_db_files before build | ✓ WIRED | `size.py:308,327` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| diff query | `p.path` / parent_path | `JOIN paths p ON p.id = a.path_id` over real `directory_sizes` rows | Yes — full path bytes back to Python prefix logic | ✓ FLOWING |
| docker hint | `p.path` | `JOIN paths` over snapshot's directory_sizes | Yes — DB-backed test asserts both paths surfaced, dockerfoo rejected | ✓ FLOWING |
| size benchmark | OLD/NEW byte totals | real collect write path (NEW) + inline legacy DDL (OLD), VACUUM + dbstat | Yes — operator measured 12.3–16.9 MB NEW vs 53–59 MB OLD on real /opt scan | ✓ FLOWING |
| collect summary | db_bytes | `PRAGMA page_count × page_size` post-commit | Yes — live WAL-mode size | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| bench modules import + expose functions | `python -c "import bench.{size,duration,churn}; hasattr checks"` | size/duration fns all True | ✓ PASS |
| D-09 gate evaluator | `evaluate_byte_budget(40900, 49152)` | `BudgetVerdict(passed=True)` | ✓ PASS |
| reduction ratio sanity | `reduction_ratio(59.3MB, 12.3MB)` | 4.82 (matches SUMMARY 4.83x @0% churn) | ✓ PASS |
| equivalence + observability + idempotency tests | `pytest test_reporting_equivalence + test_collect_observability + test_rerun_...` | 9 passed | ✓ PASS |
| Full suite | `uv run --with pytest pytest -q` | **192 passed** | ✓ PASS |

### Probe Execution

Not applicable — phase declares no `scripts/*/tests/probe-*.sh`. Verification used the unit suite + targeted spot-checks instead.

### Requirements Coverage

Phase requirement IDs (churn, path-dict, dedup-cache, schema, pragma-vacuum, reporting-equiv, docker-hints, size-harness, byte-budget, duration, observability) are **informal** — derived from locked decisions D-01..D-11. `.planning/REQUIREMENTS.md` declares no formal REQ IDs for phase 03.1 (the lone "Observability" match is an unrelated section header). Absence of traceability rows is expected for this phase per the verification brief — NOT a gap. Each informal requirement maps to a verified truth above (churn→T1/2, path-dict/schema→T3/5, dedup-cache→T4, pragma-vacuum→T6, reporting-equiv→T7/10, docker-hints→T9, size-harness→T11/12, byte-budget→T13/14, duration→artifact `duration.py`, observability→T15/16).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | — | — | None. No TBD/FIXME/XXX/HACK/PLACEHOLDER/TODO in any phase source file (schema.sql, migrations.py, connection.py, queries.py, cli.py, bench/*). No unresolved debt markers. |

Note: the `OLD_BLOB_SCHEMA_SQL` inline legacy DDL in `bench/size.py` is benchmark scaffolding to measure the pre-rewrite baseline (explicitly NOT a product compat shim — the product schema has no blob path). This is correct and consistent with the no-backward-compat policy; the product code path has no old-schema support.

### Human Verification Required

None outstanding. The single BLOCKING human-verify gate for this phase — the D-09 forensic-integrity benchmark on REAL host data — was already executed by the operator and recorded in `03.1-04-SUMMARY.md` (Task 4): reduction 3.16x–4.83x across churn 0–40%, per-snapshot 29,805–40,900 B all under the 49,152 B budget → **D-09 PASS, D-07 not triggered**. No further human verification is required for goal achievement.

### Documented Caveats (recorded, NOT gaps)

Per the verification brief and `03.1-04-SUMMARY.md`, these are documented limitations of the measurement, not unmet must-haves:
1. Measured back-to-back churn was 0; the win was bracketed via a churn SWEEP (0–40%) rather than pinned at one measured rate (D-09 explicitly flags churn=0 as the gameable best case — the sweep is the sound resolution). A future time-spaced collect series would pin the exact operating point.
2. The absolute per-snapshot byte budget (49,152 B) is normalized to the `/opt` 421-dir reference tree (≈117 B/dir/snapshot vs OLD's ≈342 B/dir/snapshot); the production-root absolute number scales with dir count.

### Gaps Summary

None. All 16 observable truths verify against the actual codebase, every required artifact exists/is substantive/is wired with real data flowing, all key links are connected, the full suite is green (192 passed), behavioral spot-checks confirm the D-09 gate evaluator and reduction ratio, the operator's D-09 PASS verdict is recorded, and no anti-patterns or debt markers exist. The phase goal — flat path-dictionary schema with a proven measured size win, equivalence-tested reporting, idempotent benchmark harnesses, and pure-JSON-preserving collect observability — is achieved. Phase 4 scheduling is unblocked.

---

_Verified: 2026-06-15_
_Verifier: Claude (gsd-verifier)_
