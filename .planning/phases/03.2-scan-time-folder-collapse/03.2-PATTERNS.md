# Phase 03.2: Scan-Time Folder Collapse - Pattern Map

**Mapped:** 2026-06-16
**Files analyzed:** 15
**Analogs found:** 15 / 15

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `src/watchdirs/config.py` | config | request-response | `src/watchdirs/config.py` | exact |
| `src/watchdirs/models.py` | model | transform | `src/watchdirs/models.py` | exact |
| `src/watchdirs/collect/scanner.py` | service | file-I/O | `src/watchdirs/collect/scanner.py` | exact |
| `src/watchdirs/db/schema.sql` | config | CRUD | `src/watchdirs/db/schema.sql` | exact |
| `src/watchdirs/db/migrations.py` | service | CRUD | `src/watchdirs/db/migrations.py` | exact |
| `src/watchdirs/reporting/queries.py` | service | CRUD | `src/watchdirs/reporting/queries.py` | exact |
| `src/watchdirs/reporting/render.py` | utility | transform | `src/watchdirs/reporting/render.py` | exact |
| `examples/senbonzakura.watchdirs.toml` | config | request-response | `examples/senbonzakura.watchdirs.toml` | exact |
| `tests/conftest.py` | test | request-response | `tests/conftest.py` | exact |
| `tests/test_cli_collect.py` | test | request-response | `tests/test_cli_collect.py` | exact |
| `tests/test_db_schema.py` | test | CRUD | `tests/test_db_schema.py` | exact |
| `tests/test_scanner_semantics.py` | test | file-I/O | `tests/test_scanner_semantics.py` | exact |
| `tests/test_reporting_queries.py` | test | CRUD | `tests/test_reporting_queries.py` | exact |
| `tests/test_reporting_equivalence.py` | test | CRUD | `tests/test_reporting_equivalence.py` | exact |
| `tests/bench/test_size.py` | test | batch | `tests/bench/test_size.py` | exact |

## Pattern Assignments

### `src/watchdirs/config.py` (config, request-response)

**Analog:** `src/watchdirs/config.py`

**Imports + dataclass pattern** ([src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:1)):
```python
from dataclasses import dataclass
from pathlib import Path
import os
import tomllib

from .models import MountPolicy
```

**Config surface pattern** ([src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:15)):
```python
@dataclass(frozen=True)
class WatchConfig:
    roots: tuple[ConfiguredRoot, ...]
    exclude_paths: tuple[Path, ...]
    mount_policy: MountPolicy
```

**Load/parse/validate flow** ([src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:63)):
```python
def load_config(path: Path) -> WatchConfig:
    config_path = Path(path).expanduser()
    data = _read_toml(config_path)
    roots = _parse_roots(data, config_path)
    exclude_paths = _parse_exclude_paths(data, config_path)
    mount_policy = _parse_mount_policy(data, config_path)
    validate_roots(roots)
    return WatchConfig(roots=roots, exclude_paths=exclude_paths, mount_policy=mount_policy)
```

**Validation/error pattern** ([src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:27), [src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:93), [src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:220)):
```python
@dataclass(frozen=True)
class ConfigError(Exception):
    kind: str
    path: str
    message: str

def _read_toml(config_path: Path) -> dict[str, object]:
    ...
    raise ConfigError("malformed_config", str(config_path), ...)

def _normalize_absolute_path(raw_path: str, error_kind: str) -> Path:
    expanded = Path(raw_path).expanduser()
    if not expanded.is_absolute():
        raise ConfigError(error_kind, raw_path, "configured paths must be absolute")
```

**Planner note:** Add `[collapse]` parsing beside `_parse_mount_policy`, return an immutable model object, and normalize `collapse.never` through the same absolute-path helper.

---

### `src/watchdirs/models.py` (model, transform)

**Analog:** `src/watchdirs/models.py`

**Model declaration pattern** ([src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:25)):
```python
@dataclass(frozen=True, slots=True)
class DirectoryAggregate:
    snapshot_id: int
    path: bytes
    parent_path: bytes | None
    depth: int
    apparent_bytes: int
    disk_bytes: int
    file_count: int
    dir_count: int
    error: str | None
```

**Small immutable policy/result types** ([src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:351), [src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:360), [src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:369), [src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:379)):
```python
@dataclass(frozen=True, slots=True)
class MountPolicy:
    skipped_filesystems: frozenset[str] = field(default_factory=frozenset)
    included_filesystems: frozenset[str] = field(default_factory=frozenset)
    skip_overlay: bool = True
    skip_namespace: bool = True
    one_filesystem: bool = True

@dataclass(frozen=True, slots=True)
class ScannerOptions:
    root: Path
    exclude_paths: tuple[Path, ...] = ()
    mount_policy: MountPolicy | None = None
```

**Reporting row model pattern** ([src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:89), [src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:120)):
```python
@dataclass(frozen=True, slots=True)
class DiffRow:
    ...
    group: GroupLabel | None = None

@dataclass(frozen=True, slots=True)
class TopRow:
    ...
    error: str | None
    group: GroupLabel | None = None
```

**Planner note:** Extend `DirectoryAggregate`, `ScannerOptions`, `TopRow`, and `DiffRow` additively with nullable collapse metadata; stay with `frozen=True, slots=True` for every new type such as `CollapsePolicy`.

---

### `src/watchdirs/collect/scanner.py` (service, file-I/O)

**Analog:** `src/watchdirs/collect/scanner.py`

**Imports + model dependency pattern** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:1)):
```python
from dataclasses import dataclass, field
import os
from pathlib import Path
import stat

from watchdirs.collect.classify import classify_mount
from watchdirs.collect.mounts import find_mount_for_path
from watchdirs.models import (
    DirectoryAggregate,
    MountDecision,
    MountInfo,
    MountPolicy,
    ScanError,
    ScanResult,
    ScannerOptions,
    SnapshotStatus,
)
```

**Iterative post-order traversal seam** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:25), [src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:44)):
```python
@dataclass(slots=True)
class _Frame:
    path_raw: bytes
    parent_path: bytes | None
    depth: int
    ...
    entries: list[os.DirEntry[bytes]] = field(default_factory=list)
    next_index: int = 0
    initialized: bool = False

def scan_root(options: ScannerOptions) -> ScanResult:
    ...
    rows: list[DirectoryAggregate] = []
    errors: list[ScanError] = []
    ...
    stack = [_Frame(...)]
```

**Initialize / pop / append row pattern** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:123), [src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:162)):
```python
if not frame.initialized:
    ...
    frame.entries = _sorted_entries(frame.path_raw)
    frame.initialized = True

if frame.next_index >= len(frame.entries):
    row = _directory_row(frame)
    stack.pop()
    rows.append(row)
    if stack:
        _merge_child(stack[-1], row)
    continue
```

**Descend/skip pattern** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:188)):
```python
if stat.S_ISDIR(entry_stat.st_mode):
    decision = should_descend(...)
    if not decision.include:
        if options.record_skipped:
            errors.append(_scan_error_message(entry_path, "mount_skipped", decision.reason))
        skipped_row = _skipped_directory_row(...)
        rows.append(skipped_row)
        _merge_child(frame, skipped_row)
        continue
    stack.append(_Frame(...))
    continue
```

**Aggregate-preserving helpers** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:381), [src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:438)):
```python
def _directory_row(frame: _Frame) -> DirectoryAggregate:
    return DirectoryAggregate(
        snapshot_id=0,
        path=frame.path_raw,
        parent_path=frame.parent_path,
        depth=frame.depth,
        apparent_bytes=frame.apparent_bytes,
        disk_bytes=frame.disk_bytes,
        file_count=frame.file_count,
        dir_count=frame.dir_count,
        error=frame.error,
    )

def _merge_child(parent: _Frame, child_row: DirectoryAggregate) -> None:
    parent.apparent_bytes += child_row.apparent_bytes
    parent.disk_bytes += child_row.disk_bytes
    parent.file_count += child_row.file_count
    parent.dir_count += child_row.dir_count + 1
```

**Error handling pattern** ([src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:140), [src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:456)):
```python
except OSError as exc:
    error = _scan_error(frame.path_raw, exc)
    errors.append(error)
    ...

def _scan_error_message(path_raw: bytes, kind: str, message: str) -> ScanError:
    return ScanError(
        path=path_raw,
        path_bytes_hex=path_raw.hex(),
        message=message,
        kind=kind,
    )
```

**Planner note:** Implement collapse by adding fields to `_Frame` and changing when subtree rows are emitted, not by replacing `_merge_child()`. The natural seam is a `row_start` marker plus late subtree truncation before the final `rows.append(row)`.

---

### `src/watchdirs/db/schema.sql` (config, CRUD)

**Analog:** `src/watchdirs/db/schema.sql`

**Table shape pattern** ([src/watchdirs/db/schema.sql](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/schema.sql:11), [src/watchdirs/db/schema.sql](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/schema.sql:20)):
```sql
CREATE TABLE IF NOT EXISTS paths (
    id INTEGER PRIMARY KEY,
    path TEXT NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS directory_sizes (
    id INTEGER PRIMARY KEY,
    snapshot_id INTEGER NOT NULL REFERENCES snapshots(id) ON DELETE CASCADE,
    path_id INTEGER NOT NULL REFERENCES paths(id),
    parent_id INTEGER REFERENCES paths(id),
    depth INTEGER NOT NULL,
    apparent_bytes INTEGER NOT NULL,
    disk_bytes INTEGER NOT NULL,
    file_count INTEGER NOT NULL,
    dir_count INTEGER NOT NULL,
    error TEXT
);
```

**Index naming pattern** ([src/watchdirs/db/schema.sql](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/schema.sql:45)):
```sql
CREATE INDEX IF NOT EXISTS directory_sizes_pathid_snapshot_idx
    ON directory_sizes(path_id, snapshot_id);

CREATE INDEX IF NOT EXISTS directory_sizes_snapshot_size_idx
    ON directory_sizes(snapshot_id, disk_bytes);
```

**Planner note:** Add collapse columns inline on `directory_sizes` and follow the existing lower-signal nullable-column style. `top_child_id` should reuse `REFERENCES paths(id)`.

---

### `src/watchdirs/db/migrations.py` (service, CRUD)

**Analog:** `src/watchdirs/db/migrations.py`

**Schema version discipline** ([src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:11), [src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:15)):
```python
SCHEMA_VERSION = 3
INSERT_BATCH_SIZE = 10000

def initialize_database(connection: sqlite3.Connection) -> None:
    user_version = int(connection.execute("PRAGMA user_version").fetchone()[0])
    if user_version > SCHEMA_VERSION:
        raise RuntimeError(...)
    if user_version == SCHEMA_VERSION:
        return
```

**Bootstrap transaction pattern** ([src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:24)):
```python
schema_sql = resources.files("watchdirs.db").joinpath("schema.sql").read_text(encoding="utf-8")
migration_script = "\n".join(
    (
        "BEGIN;",
        schema_sql,
        f"PRAGMA user_version = {SCHEMA_VERSION};",
        "COMMIT;",
    )
)
connection.executescript(migration_script)
```

**Batch insert + cache pattern** ([src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:68), [src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:102), [src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:224)):
```python
def insert_directory_rows(connection, rows, *, commit: bool = True) -> None:
    sql = """
        INSERT INTO directory_sizes (
            snapshot_id,
            path_id,
            parent_id,
            depth,
            apparent_bytes,
            disk_bytes,
            file_count,
            dir_count,
            error
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    """
    cache: dict[bytes, int] = {}
    ...

def _resolve_path_id(connection, cache: dict[bytes, int], path: bytes) -> int:
    row = connection.execute(
        "SELECT id FROM paths WHERE path = ?", (sqlite3.Binary(path),)
    ).fetchone()
    ...
```

**Snapshot lifecycle pattern** ([src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:40), [src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:185)):
```python
def create_snapshot(...):
    cursor = connection.execute(
        """
        INSERT INTO snapshots (started_at, finished_at, root_path, status, notes, error)
        VALUES (?, NULL, ?, ?, ?, NULL)
        """,
        (...),
    )

def finalize_snapshot(...):
    connection.execute(
        """
        UPDATE snapshots
        SET finished_at = ?, status = ?, notes = ?, error = ?
        WHERE id = ?
        """,
        (...),
    )
```

**Planner note:** The existing bootstrap is not enough for v3 databases. Copy the version-gate structure, but insert an explicit `user_version == 3` forward migration branch before the generic schema bootstrap path.

---

### `src/watchdirs/reporting/queries.py` (service, CRUD)

**Analog:** `src/watchdirs/reporting/queries.py`

**Report error / input validation pattern** ([src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:29), [src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:37)):
```python
class ReportError(ValueError):
    def __init__(self, code: str, message: str, **context: object) -> None:
        super().__init__(message)
        self.code = code
        self.message = message
        self.context = context
```

**Top query shape** ([src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:339)):
```python
query_rows = connection.execute(
    """
    SELECT
        p.path AS path,
        ds.depth AS depth,
        ds.apparent_bytes AS apparent_bytes,
        ds.disk_bytes AS disk_bytes,
        ds.file_count AS file_count,
        ds.dir_count AS dir_count,
        ds.error AS error
    FROM directory_sizes ds
    JOIN paths p ON p.id = ds.path_id
    WHERE ds.snapshot_id = ?
    ORDER BY ds.disk_bytes DESC, p.path ASC
    LIMIT ?
    """,
    (snapshot_id, limit),
).fetchall()
```

**Row materialization pattern** ([src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:370)):
```python
warnings_by_code_path: dict[tuple[str, bytes | None], ReportWarning] = {}
rows: list[TopRow] = []
...
rows.append(
    TopRow(
        snapshot_id=snapshot_id,
        root_path=snapshot.root_path,
        path=path,
        path_bytes_hex=path.hex(),
        ...
        group=group,
    )
)
```

**Diff query shape** ([src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:402)):
```python
WITH all_ids AS (
    SELECT path_id
    FROM directory_sizes
    WHERE snapshot_id = :baseline_id
    UNION
    SELECT path_id
    FROM directory_sizes
    WHERE snapshot_id = :current_id
)
SELECT
    p.path AS path,
    COALESCE(cp.path, pp.path) AS parent_path,
    ...
FROM all_ids a
JOIN paths p ON p.id = a.path_id
LEFT JOIN directory_sizes AS prev ...
LEFT JOIN directory_sizes AS curr ...
LEFT JOIN paths pp ON pp.id = prev.parent_id
LEFT JOIN paths cp ON cp.id = curr.parent_id
ORDER BY disk_bytes_delta DESC, depth DESC, path ASC
```

**Explain-path pattern** ([src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:509)):
```python
diff_rows, warnings = query_diff_rows(connection, pair=pair, group_by=group_by)
target = next((row for row in diff_rows if row.path == target_path), None)
if target is None:
    raise ReportError("path_not_indexed", ...)
subtree_rows = [
    row
    for row in diff_rows
    if row.path == target_path or _matches_path_prefix(row.path, target_path)
]
```

**Planner note:** Extend existing SELECTs and dataclass materializers instead of adding a new report family. Collapse metadata belongs beside current columns on `TopRow`/`DiffRow`.

---

### `src/watchdirs/reporting/render.py` (utility, transform)

**Analog:** `src/watchdirs/reporting/render.py`

**Path + payload helper pattern** ([src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:30), [src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:52)):
```python
def decode_path(path_bytes: bytes) -> str:
    return os.fsdecode(path_bytes)

def path_payload(path_bytes: bytes) -> dict[str, str]:
    return {
        "path": decode_path(path_bytes),
        "path_bytes_hex": path_bytes.hex(),
    }
```

**Top/diff payload pattern** ([src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:884), [src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:926)):
```python
def _top_row_payload(row: TopRow) -> dict[str, object]:
    payload: dict[str, object] = {
        "snapshot_id": row.snapshot_id,
        "root_path": str(row.root_path),
        **path_payload(row.path),
        ...
        "error": row.error,
        "group": _group_payload(row.group),
    }
    return payload

def _diff_row_payload(row: DiffRow) -> dict[str, object]:
    return {
        "root_path": str(row.root_path),
        **path_payload(row.path),
        ...
        "group": _group_payload(row.group),
        "error": row.error,
    }
```

**Conditional nested-object pattern** ([src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:956), [src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:976)):
```python
def _group_payload(group: GroupLabel | None) -> dict[str, object] | None:
    if group is None:
        return None
    payload: dict[str, object] = {"kind": group.kind, "key": group.key}
    if group.mount_point is not None:
        payload["mount_point"] = decode_path(group.mount_point)
    ...

def _warning_payload(warning: ReportWarning) -> dict[str, object]:
    payload: dict[str, object] = {"code": warning.code, "message": warning.message}
    if warning.path is not None:
        payload["path"] = decode_path(warning.path)
```

**Planner note:** Use the existing helper style and add collapse keys conditionally so non-collapsed rows do not leak nulls.

---

### `examples/senbonzakura.watchdirs.toml` (config, request-response)

**Analog:** `examples/senbonzakura.watchdirs.toml`

**Shipped-default example pattern** ([examples/senbonzakura.watchdirs.toml](/home/j2h4u/repos/j2h4u/watchdirs/examples/senbonzakura.watchdirs.toml:1)):
```toml
exclude_paths = [
  "/proc",
  "/sys",
  "/dev",
  "/run",
  "/tmp",
  "/dev/shm",
]

[[roots]]
path = "/"
```

**Planner note:** Add a discoverable `[collapse]` block here; keep it terse and operator-facing like the existing root sample.

---

### `tests/conftest.py` (test, request-response)

**Analog:** `tests/conftest.py`

**Fixture/export pattern** ([tests/conftest.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/conftest.py:10)):
```python
@pytest.fixture(scope="session")
def sample_config_path(repo_root: Path) -> Path:
    return repo_root / "examples" / "senbonzakura.watchdirs.toml"

@pytest.fixture
def write_config(tmp_path: Path):
    def _write_config(...):
        ...
        config_path.write_text("\n".join(lines) + ("\n" if lines else ""), encoding="utf-8")
        return config_path
```

**Planner note:** Extend `write_config()` with optional collapse parameters instead of hand-writing ad hoc TOML in each new test.

---

### `tests/test_cli_collect.py` (test, request-response)

**Analog:** `tests/test_cli_collect.py`

**CLI harness pattern** ([tests/test_cli_collect.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_cli_collect.py:21), [tests/test_cli_collect.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_cli_collect.py:48)):
```python
def run_module(repo_root: Path, *args: str, env: dict[str, str] | None = None) -> subprocess.CompletedProcess[str]:
    ...
    return subprocess.run(
        ["python3", "-m", "watchdirs", *args],
        ...
        capture_output=True,
        check=False,
    )

def assert_config_error(result: subprocess.CompletedProcess[str], expected_kind: str) -> dict[str, object]:
    assert result.returncode != 0
    payload = parse_json_output(result)
    assert payload["ok"] is False
    ...
```

**Config test style** ([tests/test_cli_collect.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_cli_collect.py:140), [tests/test_cli_collect.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_cli_collect.py:255)):
```python
config_path = write_config(raw="exclude_paths = [\"/tmp\"]\n")
result = run_module(repo_root, "collect", "--config", str(config_path), "--json")
payload = assert_config_error(result, "no_roots")

payload = tomllib.loads(sample_config_path.read_text(encoding="utf-8"))
roots = [entry["path"] for entry in payload["roots"]]
```

**Planner note:** Put `[collapse]` config acceptance/rejection coverage here, especially malformed types and normalized `never` paths.

---

### `tests/test_db_schema.py` (test, CRUD)

**Analog:** `tests/test_db_schema.py`

**Schema introspection pattern** ([tests/test_db_schema.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_db_schema.py:36), [tests/test_db_schema.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_db_schema.py:75)):
```python
user_version = connection.execute("PRAGMA user_version").fetchone()[0]
index_names = {
    row["name"]
    for row in connection.execute(
        "SELECT name FROM sqlite_master WHERE type='index' AND tbl_name='directory_sizes'"
    )
}

directory_columns = {
    row["name"]
    for row in connection.execute("PRAGMA table_info('directory_sizes')")
}
```

**Pure unit fake-connection pattern for batching** ([tests/test_db_schema.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_db_schema.py:116), [tests/test_db_schema.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_db_schema.py:160)):
```python
class RecordingConnection:
    def executemany(self, sql: str, rows) -> None:
        batch = list(rows)
        self.batches.append((sql, batch))

def test_insert_directory_rows_uses_executemany_batches(...):
    rows = [models_module.DirectoryAggregate(...)]
    connection = RecordingConnection()
    migrations_module.insert_directory_rows(connection, rows)
```

**Planner note:** Copy this file’s style for v3->v4 migration coverage, default values on existing rows, and insert tuple-shape assertions for the new columns.

---

### `tests/test_scanner_semantics.py` (test, file-I/O)

**Analog:** `tests/test_scanner_semantics.py`

**Scanner fixture pattern** ([tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:16), [tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:38)):
```python
def _scan_result(import_watchdirs_module, root: Path, **option_overrides):
    models = import_watchdirs_module("watchdirs.models")
    scanner = import_watchdirs_module("watchdirs.collect.scanner")
    options = models.ScannerOptions(...)
    return scanner.scan_root(options)

def _make_nested_fixture(root: Path) -> tuple[Path, Path]:
    child = root / "child"
    grandchild = child / "grandchild"
    grandchild.mkdir(parents=True)
    ...
```

**Behavior-first assertions** ([tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:52), [tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:127), [tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:301)):
```python
assert tuple(row.depth for row in scan_result.rows) == (2, 1, 0)
assert rows[os.fsencode(root)].file_count == 3
assert rows[os.fsencode(root)].dir_count == 2

assert scan_result.row_count == 1501
assert scan_result.rows[0].depth == 1500

assert root_row.file_count == 1
assert root_row.dir_count == 0
```

**Planner note:** New collapse tests should stay at this level: assert emitted row set, row ordering, preserved recursive bytes/counts, and shallowest-boundary behavior on concrete fixture trees.

---

### `tests/test_reporting_queries.py` (test, CRUD)

**Analog:** `tests/test_reporting_queries.py`

**Shared DB-fixture builders** ([tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:18), [tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:28), [tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:67)):
```python
def _open_db(repo_root: Path, tmp_path: Path):
    ...
    migrations_module.initialize_database(connection)
    return connection, migrations_module, models_module

def _directory_row(models_module, snapshot_id: int, path: bytes, *, disk_bytes: int, ...):
    return models_module.DirectoryAggregate(...)

def _seed_snapshot(...):
    snapshot = migrations_module.create_snapshot(connection, root_path, notes=notes)
    persisted_rows = [models_module.DirectoryAggregate(... ) for row in rows]
    if persisted_rows:
        migrations_module.insert_directory_rows(connection, persisted_rows, commit=False)
```

**Query assertion pattern** ([tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:149), [tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:224)):
```python
rows, warnings = queries.query_top_rows(connection, snapshot_id=snapshot_id, limit=4, group_by="root")
assert warnings == ()
assert [row.path for row in rows] == [...]
assert rows[0].path_bytes_hex == b"/srv".hex()
assert rows[0].root_path == Path("/srv")
```

**Planner note:** Use this suite to pin collapsed metadata in `top`, `diff`, and `explain-path` without involving CLI rendering.

---

### `tests/test_reporting_equivalence.py` (test, CRUD)

**Analog:** `tests/test_reporting_equivalence.py`

**Golden-contract pattern** ([tests/test_reporting_equivalence.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_equivalence.py:1), [tests/test_reporting_equivalence.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_equivalence.py:111), [tests/test_reporting_equivalence.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_equivalence.py:177)):
```python
_GOLDEN_DIFF = {
    b"/srv": ("grown", 200, 255, 55, None),
    ...
}

_GOLDEN_TOP_ORDER = [
    b"/srv",
    b"/srv/grow",
    ...
]
```

**Reuse sibling fixture builders** ([tests/test_reporting_equivalence.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_equivalence.py:23)):
```python
from test_reporting_queries import (
    _directory_row,
    _mount,
    _open_db,
    _seed_snapshot,
    import_module,
)
```

**Planner note:** If collapse changes row visibility semantics for report queries, add a narrow golden test here instead of recomputing expectations from the query itself.

---

### `tests/bench/test_size.py` (test, batch)

**Analog:** `tests/bench/test_size.py`

**Synthetic measurement harness pattern** ([tests/bench/test_size.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/bench/test_size.py:43), [tests/bench/test_size.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/bench/test_size.py:63)):
```python
def _scan_rows(models_module, count: int) -> list:
    rows = []
    for i in range(count):
        rows.append(
            models_module.DirectoryAggregate(
                snapshot_id=0,
                path=_PARENT + b"/segment%05d" % i,
                parent_path=_PARENT,
                depth=8,
                ...
            )
        )
    return rows

comparison = size.compare_old_vs_new(
    base_rows,
    snapshots=8,
    churn=0.02,
    workdir=tmp_path,
)
```

**Directional/storage assertions** ([tests/bench/test_size.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/bench/test_size.py:82), [tests/bench/test_size.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/bench/test_size.py:107)):
```python
assert old.pragmas == new.pragmas
assert old.page_bytes == old.stat_bytes
assert new.page_bytes == new.stat_bytes
assert new.page_bytes < old.page_bytes
assert comparison.per_snapshot_bytes > 0
```

**Planner note:** The phase-specific storage-win proof should copy this directional style: assert fewer rows / smaller bytes on deterministic fixtures, not exact production-sized numbers.

## Shared Patterns

### Immutable Dataclass Contracts
**Source:** [src/watchdirs/models.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/models.py:14)
**Apply to:** `models.py`, `config.py`, any new collapse metadata types
```python
@dataclass(frozen=True, slots=True)
class TopRow:
    ...
```

### Config Errors Return Structured JSON
**Source:** [src/watchdirs/config.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/config.py:27)
**Apply to:** new `[collapse]` parsing and CLI-facing config validation
```python
@dataclass(frozen=True)
class ConfigError(Exception):
    kind: str
    path: str
    message: str
```

### Scanner Preserves Recursive Aggregates Through `_merge_child`
**Source:** [src/watchdirs/collect/scanner.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/collect/scanner.py:438)
**Apply to:** collapse implementation in `scanner.py`
```python
def _merge_child(parent: _Frame, child_row: DirectoryAggregate) -> None:
    parent.apparent_bytes += child_row.apparent_bytes
    parent.disk_bytes += child_row.disk_bytes
    parent.file_count += child_row.file_count
    parent.dir_count += child_row.dir_count + 1
```

### Path Dictionary Reuse for Any Path Reference
**Source:** [src/watchdirs/db/migrations.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/db/migrations.py:102)
**Apply to:** `top_child_id` persistence and any new path FK
```python
def _resolve_path_id(connection, cache: dict[bytes, int], path: bytes) -> int:
    row = connection.execute(
        "SELECT id FROM paths WHERE path = ?", (sqlite3.Binary(path),)
    ).fetchone()
```

### Query Layer Owns SQL + Dataclass Materialization
**Source:** [src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:351), [src/watchdirs/reporting/queries.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/queries.py:424)
**Apply to:** collapse metadata exposure in `top`, `diff`, and `explain-path`
```sql
SELECT
    p.path AS path,
    ...
FROM directory_sizes ds
JOIN paths p ON p.id = ds.path_id
```

### Render Layer Conditionally Emits Optional Nested Fields
**Source:** [src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:956), [src/watchdirs/reporting/render.py](/home/j2h4u/repos/j2h4u/watchdirs/src/watchdirs/reporting/render.py:976)
**Apply to:** omit null collapse fields from JSON payloads
```python
if group.mount_point is not None:
    payload["mount_point"] = decode_path(group.mount_point)
...
if warning.path is not None:
    payload["path"] = decode_path(warning.path)
```

### Test Fixtures Stay Local and Explicit
**Source:** [tests/test_reporting_queries.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_reporting_queries.py:67), [tests/test_scanner_semantics.py](/home/j2h4u/repos/j2h4u/watchdirs/tests/test_scanner_semantics.py:38)
**Apply to:** all new collapse tests
```python
def _seed_snapshot(...):
    ...

def _make_nested_fixture(root: Path) -> tuple[Path, Path]:
    ...
```

## No Analog Found

None. Every scoped file in Phase 03.2 already has a direct in-repo analog because this phase extends existing config, scanner, schema, reporting, and test seams rather than introducing a new subsystem.

## Metadata

**Analog search scope:** `src/watchdirs/`, `examples/`, `tests/`
**Files scanned:** 15 primary files plus targeted helper ranges
**Pattern extraction date:** 2026-06-16
