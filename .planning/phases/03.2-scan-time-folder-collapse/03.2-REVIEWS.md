# Phase 03.2 Cross-AI Plan Reviews

**Phase:** 03.2 - Scan-Time Folder Collapse  
**Cycle:** 1  
**Reviewers:** OpenCode 1.17.6, Claude Code 2.1.177  
**Created:** 2026-06-16

## Cycle 1 Summary

Two unresolved HIGH findings remain. The plans are strong overall, but reviewers found correctness interactions at the boundaries between collapse and existing filesystem/reporting invariants that must be made executable in PLAN.md before `/gsd-execute-phase`.

### Current HIGH Concerns

- **H1 — Storage-domain totals may be wrong with collapsed rows.** OpenCode flagged that `query_indexed_storage_domain_totals()` is not mentioned in the plans. Collapsed rows keep recursive bytes while descendant rows are absent, and the existing parent/domain walk may misattribute a collapsed subtree when intermediate rows are missing. PLAN.md needs a task or acceptance criterion that audits/tests collapsed-row interaction with storage-domain totals.
- **H2 — `collapse.never` can be defeated by a collapsing ancestor.** Claude flagged that plans only block collapse when the current node itself matches `collapse.never`. If a protected path is nested under an ancestor that qualifies by name/fan-out/descendant-count, row-buffer truncation can still drop the protected subtree. PLAN.md needs an explicit semantic decision and tests. Recommended decision: a `never` path poisons ancestors on its path, so any ancestor containing a protected descendant stays expanded.

### Current Actionable Non-HIGH Concerns

- **A1 — Preserve or explicitly surface partial-error/skipped-mount evidence under collapsed subtrees.** Claude flagged that `del rows[frame.row_start:]` can delete skipped-mount or partial-error evidence rows inside a collapsed subtree, undermining FSEM-05. PLAN.md needs a tested decision: preserve evidence rows, summarize evidence on the boundary row, or explicitly document/test that collapse hides descendant evidence.
- **A2 — Prove migration idempotency and interruption behavior.** Claude flagged that the v3-to-v4 migration plan does not test rerun/partial-state behavior. PLAN.md should require tests that re-running migration succeeds and that partial/duplicate-column states cannot advance `user_version` incorrectly.
- **A3 — Add an end-to-end scanner-to-storage size proof.** Claude flagged that Plan 04's storage proof constructs collapsed rows by hand. PLAN.md should require at least one synthetic filesystem fixture scanned with real `scan_root()` under collapse and no-collapse policies, then inserted through product SQLite paths to prove row-count reduction and aggregate equality.
- **A4 — Stress-test row-buffer contiguity with mixed subtrees.** Claude flagged that the contiguous-suffix assumption behind `del rows[frame.row_start:]` is load-bearing. PLAN.md should require a mixed subtree test with normal child dirs plus skipped/error rows, tied to the intended evidence behavior from A1.
- **A5 — Pin path-component-aware `collapse.never` matching.** Claude flagged substring risk such as `/data` matching `/database`. PLAN.md should require exact-or-descendant path-component semantics and a `/data` vs `/database` test.
- **A6 — Clarify collapsed `top_child` chain semantics.** OpenCode noted that a collapsed row's `top_child` may itself be collapsed. PLAN.md should document this as intentional, if accepted, and ensure explain/report payloads remain understandable.
- **A7 — Avoid inefficient or incomplete explain-path collapsed-ancestor lookup.** OpenCode noted that explain-path boundary resolution may scan a limited result set. PLAN.md should specify a direct query for the deepest collapsed ancestor or otherwise prove the existing query path is complete enough.
- **A8 — Handle changed collapse policy across snapshots in diff metadata.** OpenCode flagged current-vs-baseline metadata fallback edge cases when a path is collapsed in one snapshot and expanded in another. PLAN.md should add an acceptance criterion or test for current-first/baseline fallback semantics.
- **A9 — Keep collapse size proof separate from the old blob-vs-dictionary benchmark.** OpenCode noted the benchmark helper must compare collapsed and uncollapsed product-schema DBs only. PLAN.md mostly says this, but the validation should assert it.
- **A10 — Add exact `collapsed_dirs` threshold/count convention tests.** Claude flagged possible off-by-one ambiguity around descendant count and stored `collapsed_dirs`. PLAN.md should pin whether `frame.dir_count` excludes the collapsed directory and test threshold equality.
- **A11 — Trim per-plan artifact lists to each plan's real write scope.** Claude flagged that every plan currently repeats the whole phase artifact list, which can mislead executors and source grounding. PLAN.md files should list only artifacts produced by that specific plan, while a phase-wide audit can remain elsewhere.

## OpenCode Review

**Overall risk:** LOW.

OpenCode found the plan sequence strong and migration discipline sound. It raised one HIGH concern:

- `query_indexed_storage_domain_totals()` may misattribute collapsed subtree bytes when intermediate rows are absent; add a domain-total collapse audit test or a Plan 03 task.

Actionable non-HIGH OpenCode findings:

- Document that `top_child` can point to another collapsed row.
- Ensure explain-path collapsed-boundary lookup is complete, not limited by a broad result set.
- Test diff metadata when collapse policies differ between snapshots.
- Keep product-schema collapse size proof separate from old blob-vs-dictionary benchmark comparisons.

## Claude Review

**Overall risk:** MEDIUM.

Claude found one HIGH correctness gap:

- `collapse.never` must protect nested paths even when an ancestor independently qualifies for collapse, or the limitation must be explicitly documented. Recommended: a protected descendant prevents ancestor collapse.

Actionable non-HIGH Claude findings:

- Decide and test how collapse interacts with partial-error and skipped-mount evidence rows.
- Test migration idempotency and interruption/partial-state behavior.
- Add an end-to-end `scan_root()` to SQLite storage-size proof.
- Stress-test row-buffer truncation with mixed child/error/skipped rows.
- Pin path-component-aware `never` matching.
- Trim per-plan artifact lists to each plan's true write scope.
- Document direct-child-only `top_child` limitation when a direct file dominates.
- State v1/v2 DB behavior explicitly if only v3 and v0 bootstrap are supported.
- Pin `collapsed_dirs` off-by-one semantics.

## Verification Coverage

Source-grounding authority: `grep` over repository files.

### Verified Existing Symbols And Files

- `src/watchdirs/collect/scanner.py` — verified existing scanner module and helper symbols including `scan_root`, `_Frame`, `_directory_row`, `_merge_child`, and skip/error handling references.
- `src/watchdirs/db/migrations.py` — verified existing `SCHEMA_VERSION`, `initialize_database`, `insert_directory_rows`, and `_resolve_path_id`.
- `src/watchdirs/db/schema.sql` — verified existing `paths` and `directory_sizes` tables.
- `src/watchdirs/config.py` — verified existing `WatchConfig`, `load_config`, `ConfigError`, and path-normalization helpers.
- `src/watchdirs/models.py` — verified existing `DirectoryAggregate`, `ScannerOptions`, `TopRow`, and `DiffRow`.
- `src/watchdirs/reporting/queries.py` — verified existing `query_top_rows`, `query_diff_rows`, `query_explain_path_rows`, and `query_indexed_storage_domain_totals`.
- `src/watchdirs/reporting/render.py` — verified existing reporting render module.
- Test files named in the plans exist: `tests/test_cli_collect.py`, `tests/test_db_schema.py`, `tests/test_scanner_semantics.py`, `tests/test_reporting_queries.py`, `tests/test_reporting_equivalence.py`, `tests/bench/test_size.py`.

### Declared New Artifacts Excluded From Missing-Symbol Failure

These are expected to be created by Phase 03.2 and should not be treated as hallucinated existing symbols:

- `CollapsePolicy`
- `WatchConfig.collapse_policy`
- `ScannerOptions.collapse_policy`
- `DirectoryAggregate.collapsed`
- `DirectoryAggregate.collapse_reason`
- `DirectoryAggregate.collapsed_dirs`
- `DirectoryAggregate.top_child_path`
- `DirectoryAggregate.top_child_disk_bytes`
- `TopRow.collapsed`, `TopRow.collapse_reason`, `TopRow.collapsed_dirs`, `TopRow.top_child_path`, `TopRow.top_child_disk_bytes`
- `DiffRow.collapsed`, `DiffRow.collapse_reason`, `DiffRow.collapsed_dirs`, `DiffRow.top_child_path`, `DiffRow.top_child_disk_bytes`
- `DEFAULT_COLLAPSE_NAMES`
- `_parse_collapse_policy`
- `SCHEMA_VERSION = 4`
- `_migrate_v3_to_v4`
- `directory_sizes.collapsed`
- `directory_sizes.collapse_reason`
- `directory_sizes.collapsed_dirs`
- `directory_sizes.top_child_id`
- `directory_sizes.top_child_disk_bytes`
- `_collapse_metadata_payload`
- `CollapseComparison`
- `compare_uncollapsed_vs_collapsed`

### Uncheckable / Skipped

- Signature-level compatibility for planned new dataclass fields is UNCHECKABLE under grep authority.
- Exact SQL result behavior for not-yet-written collapsed-row queries is UNCHECKABLE until implementation.
- External reviewer claims about runtime performance are advisory; no benchmark was executed during review.

## Cycle 2 Summary

**Reviewers:** OpenCode 1.17.6, Claude Code 2.1.177  
**Result:** Previous HIGH findings H1/H2 are incorporated. Two actionable MEDIUM findings remain.

### Current HIGH Concerns

None.

### Current Actionable Non-HIGH Concerns

- **M1 — H1 storage-domain reconciliation has tests but an under-specified GREEN path.** Claude agreed the finding is represented, but Plan 03 Task 1 should explicitly permit changing `query_indexed_storage_domain_totals()` aggregation/subtraction logic and state the GREEN invariant: collapsed/expanded indexed boundary rows reconcile to the root recursive aggregate with no double counting, including nested storage-domain boundaries below missing intermediate paths.
- **M2 — Folded skipped/error evidence aggregation needs bounded format and reporting participation checks.** Claude agreed evidence is represented, but Plan 02 Task 2 should cap/aggregate boundary evidence as a stable marker plus counts rather than an unbounded concatenated string, and add an assertion that collapsed boundary rows with folded evidence still participate normally in top/report/storage-domain rows.

### Reviewer Notes Considered Non-Blocking

- OpenCode reported no new HIGH or MEDIUM concerns and called the plans ready for execution.
- OpenCode's LOW observations about `churn`, evidence summary format, and explain-path implementation mechanics are covered by M1/M2 or can be handled during execution.
- Claude's LOW notes about direct-file `top_child`, helper-symbol source grounding, and traversal cost do not require new plan changes because they are either already documented as directory-only scope, execution read-first checks, or known scan-cost behavior.

## OpenCode Review — Cycle 2

OpenCode found all eleven Cycle 1 findings explicitly addressed and incorporated across the four plans. It reported no HIGH or MEDIUM concerns.

LOW observations:

- `compare_uncollapsed_vs_collapsed()` includes a `churn` parameter whose semantics are copied from existing benchmark helpers.
- Folded evidence summary format should be pinned during implementation.
- Explain-path direct DB query should clearly replace or supplement the existing pre-filter.

## Claude Review — Cycle 2

Claude found all eleven Cycle 1 findings incorporated into PLAN.md, with two remaining MEDIUM concerns:

- **M1:** Plan 03 should explicitly permit modifying `query_indexed_storage_domain_totals()` and state the reconciliation invariant, not only require tests.
- **M2:** Plan 02 should bound folded evidence aggregation and verify collapsed boundary rows with folded evidence still participate normally in top/storage-domain reporting.

LOW notes:

- `top_child` is intentionally directory-only, but a one-line note would reduce ambiguity.
- Executor should confirm helper names in read-first files before writing tests.
- Collapse reduces stored rows, not traversal cost.

## Cycle 3 Final Convergence Summary

**Reviewers:** OpenCode 1.17.6, Claude Code 2.1.177  
**Result:** Converged.

### Current HIGH Concerns

None.

### Current Actionable Non-HIGH Concerns

None.

### Resolution Notes

- **M1 resolved:** Plan 03 Task 1 now explicitly permits modifying `query_indexed_storage_domain_totals()` aggregation/subtraction logic if RED tests fail. It states the GREEN invariant in task behavior, action, acceptance criteria, success criteria, and validation: collapsed and expanded indexed boundary rows reconcile to the root recursive aggregate with no double counting, including nested storage-domain boundaries below missing intermediate paths.
- **M2 resolved:** Plan 02 Task 2 now defines bounded folded evidence as a stable marker plus total/per-kind counts, forbids descendant path/message concatenation in the boundary `error`, and retains exact paths in `ScanResult.errors`. Plan 03 Task 1 verifies collapsed boundary rows with folded evidence still participate normally in top/report query rows and storage-domain totals.

## OpenCode Review — Cycle 3

OpenCode reported:

- M1 converged.
- M2 converged.
- No HIGH or actionable MEDIUM/LOW concerns remain outside PLAN.md.
- Plans are converged and ready for execution.

## Claude Review — Cycle 3

Claude reported:

- Verdict: converged.
- No HIGH or actionable MEDIUM/LOW concerns remain outside PLAN.md.
- Both Cycle 2 MEDIUM findings are explicitly incorporated into the plan contracts.
- No further plan changes are required before execution.
