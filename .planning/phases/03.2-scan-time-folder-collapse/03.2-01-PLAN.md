---
phase: "03.2-scan-time-folder-collapse"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/watchdirs/models.py
  - src/watchdirs/config.py
  - src/watchdirs/db/schema.sql
  - src/watchdirs/db/migrations.py
  - tests/conftest.py
  - tests/test_cli_collect.py
  - tests/test_db_schema.py
  - examples/senbonzakura.watchdirs.toml
autonomous: true
requirements:
  - collapse-list
  - collapse-row-flag
  - depth-fanout-safety-net
must_haves:
  truths:
    - "Configured [collapse] defaults exist and parse into an immutable CollapsePolicy per D-03, D-04, D-05, D-06, D-07, and D-10."
    - "Fresh and existing SQLite databases expose schema version 4 with collapse metadata columns per D-08 and D-09."
    - "Existing version 3 databases are altered in place before user_version advances to 4."
  artifacts:
    - path: "src/watchdirs/models.py"
      provides: "CollapsePolicy and additive collapse metadata on DirectoryAggregate/ScannerOptions"
    - path: "src/watchdirs/config.py"
      provides: "[collapse] TOML parsing and defaults"
    - path: "src/watchdirs/db/schema.sql"
      provides: "fresh directory_sizes collapse columns"
    - path: "src/watchdirs/db/migrations.py"
      provides: "SCHEMA_VERSION 4 and explicit v3 to v4 SQLite migration"
  key_links:
    - from: "src/watchdirs/config.py"
      to: "src/watchdirs/models.py"
      via: "load_config returns WatchConfig.collapse_policy"
      pattern: "collapse_policy"
    - from: "src/watchdirs/db/migrations.py"
      to: "src/watchdirs/db/schema.sql"
      via: "initialize_database creates or migrates the same directory_sizes columns"
      pattern: "SCHEMA_VERSION = 4"
---

<objective>
Create the Phase 03.2 foundation: the typed collapse policy, the `[collapse]` config surface with shipped defaults, and the explicit SQLite schema version 4 migration.

Purpose: This plan makes scan-time collapse configurable and durable before scanner/reporting behavior depends on the new fields.
Output: `CollapsePolicy`, `WatchConfig.collapse_policy`, `ScannerOptions.collapse_policy`, schema version 4 collapse columns, tests proving config validation and v3 to v4 migration.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-VALIDATION.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md
@.planning/phases/03.1-storage-efficiency/03.1-CONTEXT.md
@.planning/phases/03.1-storage-efficiency/03.1-02-SUMMARY.md
@src/watchdirs/models.py
@src/watchdirs/config.py
@src/watchdirs/db/schema.sql
@src/watchdirs/db/migrations.py
@tests/conftest.py
@tests/test_cli_collect.py
@tests/test_db_schema.py
@examples/senbonzakura.watchdirs.toml
</context>

## Artifacts This Plan Produces

- `src/watchdirs/models.py`: `CollapsePolicy`, `DirectoryAggregate` collapse metadata, and `ScannerOptions.collapse_policy`.
- `src/watchdirs/config.py`: `DEFAULT_COLLAPSE_NAMES`, `_parse_collapse_policy()`, and `WatchConfig.collapse_policy`.
- `src/watchdirs/db/schema.sql`: schema version 4 `directory_sizes` collapse columns.
- `src/watchdirs/db/migrations.py`: `SCHEMA_VERSION = 4`, `_migrate_v3_to_v4()`, schema-column verification, and collapse metadata insert support.
- `tests/conftest.py`, `tests/test_cli_collect.py`, `tests/test_db_schema.py`: config and migration coverage for this plan's contracts.
- `examples/senbonzakura.watchdirs.toml`: shipped `[collapse]` defaults.

## Review Feedback Incorporated

| Finding | Plan Contract |
| --- | --- |
| A2 | Task 2 requires idempotent reruns and partial-state tests before `user_version` can advance. |
| A11 | This section lists only Plan 01 artifacts; phase-wide coverage remains in the source audit. |

## Phase Source Audit

| Source | ID | Required item | Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | - | Collapse never-interesting subtrees at scan time, keep recursive aggregate, drop descendants, fit retained history budget | 01, 02, 04 | COVERED |
| REQ | collapse-list | Configurable collapse list and shipped defaults | 01, 02 | COVERED |
| REQ | collapse-aggregate-preserved | Boundary row keeps recursive bytes/counts while descendants disappear | 02, 04 | COVERED |
| REQ | collapse-row-flag | Persist and expose collapsed metadata and breadcrumb | 01, 03 | COVERED |
| REQ | depth-fanout-safety-net | Fan-out and descendant-count triggers, no depth trigger | 01, 02 | COVERED |
| RESEARCH | migration | Explicit SQLite v3 to v4 migration; no ORM schema-push task | 01 | COVERED |
| RESEARCH | top-child-size | Resolve D-08 vs D-09 by adding `top_child_disk_bytes` | 01, 02, 03 | COVERED |
| RESEARCH | storage-proof | Cardinality and per-snapshot byte reduction proof | 04 | COVERED |
| CONTEXT | D-01 | Irreversible scan-time collapse | 02, 04 | COVERED |
| CONTEXT | D-02 | Shallowest qualifying boundary | 02 | COVERED |
| CONTEXT | D-03 | Known-noise names | 01, 02 | COVERED |
| CONTEXT | D-04 | Fan-out threshold default 500 | 01, 02 | COVERED |
| CONTEXT | D-05 | Descendant-count default 10000 | 01, 02 | COVERED |
| CONTEXT | D-06 | Depth is not a trigger | 01, 02 | COVERED |
| CONTEXT | D-07 | `collapse.never` allowlist | 01, 02 | COVERED |
| CONTEXT | D-08 | Single largest direct-child breadcrumb with bytes | 01, 02, 03 | COVERED |
| CONTEXT | D-09 | Collapsed-row schema metadata | 01, 03 | COVERED |
| CONTEXT | D-10 | `[collapse]` table defaults | 01 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed collapse policy and TOML parsing</name>
  <files>src/watchdirs/models.py, src/watchdirs/config.py, tests/conftest.py, tests/test_cli_collect.py, examples/senbonzakura.watchdirs.toml</files>
  <read_first>AGENTS.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md, src/watchdirs/models.py, src/watchdirs/config.py, tests/conftest.py, tests/test_cli_collect.py, examples/senbonzakura.watchdirs.toml</read_first>
  <behavior>
    - RED: `load_config()` returns `WatchConfig.collapse_policy` with default `names`, `fan_out=500`, `descendants=10000`, and `never=()` when `[collapse]` is absent per D-03, D-04, D-05, D-07, and D-10.
    - RED: explicit `[collapse]` values override defaults, `collapse.never` entries normalize to absolute `Path` values, and non-string names, non-positive thresholds, or relative `never` paths raise `ConfigError("malformed_config" or "invalid_collapse_never")`.
    - RED: depth has no config key and no `CollapsePolicy` field per D-06.
  </behavior>
  <action>Add `CollapsePolicy` to `src/watchdirs/models.py` with `names: frozenset[str]`, `fan_out: int`, `descendants: int`, and `never: tuple[Path, ...]`. Add nullable/default collapse metadata fields to `DirectoryAggregate` now so downstream tests can construct rows: `collapsed: bool = False`, `collapse_reason: str | None = None`, `collapsed_dirs: int | None = None`, `top_child_path: bytes | None = None`, and `top_child_disk_bytes: int | None = None`. Add `ScannerOptions.collapse_policy: CollapsePolicy | None = None`. In `src/watchdirs/config.py`, add `DEFAULT_COLLAPSE_NAMES` exactly containing `node_modules`, `.venv`, `.git`, `site-packages`, `__pycache__`, `.cache`, `.mypy_cache`, `.pytest_cache`, `.tox`, `.npm`, `.gradle`, `.cargo`, and `.rustup`; add `_parse_collapse_policy(data, config_path)` and wire it into `load_config()`. Validate `collapse.names` as non-empty strings after stripping whitespace, `collapse.fan_out` and `collapse.descendants` as integers `>= 1`, and `collapse.never` as absolute paths using the existing normalization helper. Update `tests/conftest.py` `write_config()` to optionally emit a `[collapse]` table for tests. Update `examples/senbonzakura.watchdirs.toml` to include the shipped `[collapse]` defaults as live TOML values, not comments, so operators can inspect the active policy.</action>
  <acceptance_criteria>`tests/test_cli_collect.py` asserts default and explicit collapse policy parsing, rejects malformed `[collapse]`, confirms no depth field exists, and the example config parses with the shipped D-10 values.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_cli_collect.py -x</automated>
  </verify>
  <done>`WatchConfig.collapse_policy` is populated for every config, malformed collapse config fails with the existing JSON config-error envelope, and no report-time display controls or per-root override keys are introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema version 4 collapse columns and explicit migration</name>
  <files>src/watchdirs/db/schema.sql, src/watchdirs/db/migrations.py, tests/test_db_schema.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, .planning/phases/03.1-storage-efficiency/03.1-02-SUMMARY.md, src/watchdirs/db/schema.sql, src/watchdirs/db/migrations.py, tests/test_db_schema.py</read_first>
  <behavior>
    - RED: a fresh database initializes at `SCHEMA_VERSION == 4` and `directory_sizes` contains `collapsed`, `collapse_reason`, `collapsed_dirs`, `top_child_id`, and `top_child_disk_bytes` per D-08 and D-09.
    - RED: an existing schema version 3 database gains the same columns through `ALTER TABLE` before `PRAGMA user_version` is set to 4.
    - RED: inserting a collapsed `DirectoryAggregate` resolves `top_child_path` through `paths` into `top_child_id` and stores `top_child_disk_bytes`.
    - RED: running initialization twice against an already-upgraded database is a no-op, and a version 3 database with some collapse columns already present completes without duplicate-column errors.
    - RED: a partially altered database cannot be marked version 4 unless every required collapse column exists with the expected defaults and nullable foreign-key shape.
  </behavior>
  <action>Bump `SCHEMA_VERSION` to `4`. Extend `src/watchdirs/db/schema.sql` `directory_sizes` with inline columns: `collapsed INTEGER NOT NULL DEFAULT 0`, `collapse_reason TEXT`, `collapsed_dirs INTEGER`, `top_child_id INTEGER REFERENCES paths(id)`, and `top_child_disk_bytes INTEGER`. This plan resolves the D-08/D-09 open question by storing `top_child_disk_bytes` because D-08 requires the selected direct child path plus bytes; use disk bytes because top reporting and growth triage are disk-pressure oriented. In `initialize_database()`, branch explicitly when `user_version == 3`: run `_migrate_v3_to_v4(connection)` inside a transaction. `_migrate_v3_to_v4()` must read `PRAGMA table_info('directory_sizes')`, add only missing collapse columns, verify the complete required column set, and set `PRAGMA user_version = 4` only after verification succeeds. Re-running the migration against version 4 must return cleanly. A database at schema versions 1 or 2 is not a valid input to this phase unless it has already passed the earlier dictionary migration; detect that shape and raise a clear unsupported-schema error instead of stamping it as version 4. Keep version `0` bootstrap using `schema.sql`; keep `user_version > SCHEMA_VERSION` fail-fast. Update `_directory_row_values()` and `insert_directory_rows()` to insert `collapsed`, `collapse_reason`, `collapsed_dirs`, `top_child_id`, and `top_child_disk_bytes`, resolving `top_child_path` through `_resolve_path_id()` only when non-null. Do not add Prisma, Drizzle, Payload, Supabase, TypeORM, or any ORM schema-push workflow; this repo uses direct SQLite DDL and migration tests. Addresses review concern A2.</action>
  <acceptance_criteria>`tests/test_db_schema.py` proves fresh schema version 4, explicit version 3 to 4 migration, idempotent rerun, partial-column recovery, no `user_version` advancement on invalid partial state, default values on preexisting rows, and collapsed row insertion with `top_child_id` + `top_child_disk_bytes`.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_db_schema.py -x</automated>
  </verify>
  <done>Fresh and existing SQLite databases expose the same collapse metadata columns, no existing version 3 DB can be marked version 4 without the columns, and path/breadcrumb values are bound as SQL parameters through existing helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| TOML config -> config parser | Operator-controlled `[collapse]` values become scanner policy. |
| migration code -> existing SQLite DB | Version 3 files are mutated in place and then marked version 4. |
| scanner row metadata -> SQLite insert | Raw path bytes and top-child references cross into SQL storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03.2-01 | Tampering | `config.py` `[collapse]` parser | mitigate | Strict type/range validation for `names`, `fan_out`, `descendants`, and absolute `never` paths; malformed config exits through `ConfigError`. |
| T-03.2-02 | Tampering | `migrations.py` v3 to v4 upgrade | mitigate | Transaction-scoped `ALTER TABLE`, post-migration column assertions, and tests that fail if `user_version` advances without columns. |
| T-03.2-03 | Tampering | `top_child_id` insertion | mitigate | Resolve `top_child_path` through `_resolve_path_id()` and bind all path values as `sqlite3.Binary` parameters. |
| T-03.2-04 | Denial of Service | missed collapse policy | mitigate | Defaults include known high-cardinality names and thresholds; scanner behavior is implemented in Plan 02 and proven in Plan 04. |
| T-03.2-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan; package legitimacy audit is not triggered. |
</threat_model>

<verification>
Run `uv run --with pytest pytest tests/test_cli_collect.py tests/test_db_schema.py -q` after both tasks, then run `uv run --with pytest pytest -q -x` as the wave-1 sampling gate.
</verification>

<success_criteria>
- `CollapsePolicy` and schema version 4 exist with tests.
- D-08 is implemented with `top_child_disk_bytes`, not narrowed to path-only metadata.
- Version 3 databases migrate through idempotent explicit SQLite DDL and initialization verification.
- No ORM/schema-push task or new dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md` when done.
</output>
