---
phase: "03.2-scan-time-folder-collapse"
plan: "02"
type: tdd
wave: 2
depends_on:
  - "03.2-01"
files_modified:
  - src/watchdirs/collect/scanner.py
  - tests/test_scanner_semantics.py
autonomous: true
requirements:
  - collapse-list
  - collapse-aggregate-preserved
  - depth-fanout-safety-net
  - collapse-row-flag
must_haves:
  truths:
    - "Collapsed boundary rows preserve recursive apparent bytes, disk bytes, file count, and descendant directory count per D-01."
    - "Descendant rows below a collapsed boundary are not emitted, and the shallowest qualifying boundary wins per D-02."
    - "Known-name, fan-out, descendant-count, and never-collapse rules behave per D-03, D-04, D-05, D-06, and D-07."
    - "A `collapse.never` protected descendant prevents every ancestor on its path from collapsing."
    - "Folded partial-error or skipped-mount evidence is surfaced on the collapsed boundary instead of disappearing silently."
  artifacts:
    - path: "src/watchdirs/collect/scanner.py"
      provides: "scan-time collapse semantics"
    - path: "tests/test_scanner_semantics.py"
      provides: "behavioral scanner collapse tests"
  key_links:
    - from: "src/watchdirs/collect/scanner.py"
      to: "src/watchdirs/models.py"
      via: "ScannerOptions.collapse_policy controls DirectoryAggregate collapse fields"
      pattern: "collapse_policy"
    - from: "src/watchdirs/collect/scanner.py"
      to: "src/watchdirs/db/migrations.py"
      via: "DirectoryAggregate.top_child_path/top_child_disk_bytes feed insert_directory_rows"
      pattern: "top_child"
---

<objective>
Implement scan-time folder collapse in the scanner while preserving the existing recursive aggregate math.

Purpose: This is the storage-cardinality behavior of Phase 03.2: collapse once at the shallowest qualifying directory, keep that directory's recursive aggregate, and drop descendant rows.
Output: Scanner collapse behavior and tests for known names, fan-out, descendant-count, `never`, shallowest-boundary semantics, aggregate preservation, and top-child metadata.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-VALIDATION.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md
@.planning/phases/03.1-storage-efficiency/03.1-02-SUMMARY.md
@src/watchdirs/collect/scanner.py
@src/watchdirs/models.py
@tests/test_scanner_semantics.py
@.planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md
</context>

## Artifacts This Plan Produces

- `src/watchdirs/collect/scanner.py`: shallowest-boundary collapse evaluation, protected-descendant detection, evidence surfacing, `known_noise`/`fan_out`/`descendant_count` reasons, top-child selection, and aggregate-preserving row truncation.
- `tests/test_scanner_semantics.py`: scanner tests for collapse triggers, protected descendants, path-component-aware `never`, folded evidence, threshold equality, `collapsed_dirs`, and top-child metadata.

## Review Feedback Incorporated

| Finding | Plan Contract |
| --- | --- |
| H2 | Task 1 makes protected descendants poison ancestor collapse. |
| A1/A4 | Task 2 requires mixed-subtree tests proving folded error/skipped evidence is surfaced on the boundary and retained in `ScanResult.errors`. |
| A5 | Task 1 requires path-component-aware `collapse.never` matching with `/data` versus `/database`. |
| A10 | Task 2 pins `collapsed_dirs` as folded descendant directory count excluding the boundary and tests threshold equality. |
| A11 | This section lists only Plan 02 artifacts. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse known-noise and allowlisted subtrees at the shallowest boundary</name>
  <files>src/watchdirs/collect/scanner.py, tests/test_scanner_semantics.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md, src/watchdirs/collect/scanner.py, src/watchdirs/models.py, tests/test_scanner_semantics.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md</read_first>
  <behavior>
    - RED: a tree containing `node_modules` collapses that directory into one row with `collapsed=True`, `collapse_reason="known_noise"`, `collapsed_dirs` equal to folded descendant directory count, full recursive bytes/counts preserved, and no descendant rows emitted per D-01 and D-03.
    - RED: when both a parent and child qualify, only the shallowest qualifying parent is persisted as collapsed per D-02.
    - RED: a path under `collapse.never` remains fully expanded even if its basename is in `collapse.names`, protecting the allowlisted subtree per D-07.
    - RED: if `/srv/cache` qualifies for collapse but `/srv/cache/data/protected` is listed in `collapse.never`, `/srv/cache` and ancestors on that path remain expanded while unrelated qualifying siblings can still collapse.
    - RED: `collapse.never` uses exact-or-descendant path-component matching, so a protected `/data` path does not protect `/database`.
  </behavior>
  <action>Extend `_Frame` in `src/watchdirs/collect/scanner.py` with `row_start`, `collapse_reason`, `direct_child_dir_count`, `top_child_path`, and `top_child_disk_bytes`. Add path-boundary helpers for collapse policy matching over raw bytes. A path is protected when it exactly equals a `collapse.never` path, is below a `collapse.never` path, or is an ancestor of a configured `collapse.never` path; that last ancestor-poisoning rule resolves H2. Component boundaries are mandatory: `/data` protects `/data` and `/data/x`, but not `/database`. At frame initialization, set `row_start = len(rows)` and set `collapse_reason = "known_noise"` only when the basename decoded through filesystem bytes matches `CollapsePolicy.names` and the path is not protected. Keep traversal and `_merge_child()` aggregate math intact. When a child directory row is merged, update the parent top-child fields if that direct child's `disk_bytes` is greater than the current top child, using the child directory path and recursive disk bytes. At frame pop, if `collapse_reason` is set, delete only the foldable descendant row slice, then append one `DirectoryAggregate` marked `collapsed=True`, `collapse_reason=frame.collapse_reason`, `collapsed_dirs=frame.dir_count`, `top_child_path=frame.top_child_path`, and `top_child_disk_bytes=frame.top_child_disk_bytes`. Do not use depth as a collapse trigger per D-06. Addresses review concerns H2 and A5.</action>
  <acceptance_criteria>`tests/test_scanner_semantics.py` has tests for known-noise collapse, recursive aggregate equality against a no-collapse policy, shallowest-boundary collapse, protected-descendant ancestor poisoning, `/data` versus `/database` component matching, and top-child path plus disk bytes on the collapsed row.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_scanner_semantics.py -x</automated>
  </verify>
  <done>Known-noise collapse is irreversible in persisted rows, keeps boundary aggregates, records `known_noise`, and never emits descendant rows below the collapsed boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fan-out and descendant-count safety net semantics</name>
  <files>src/watchdirs/collect/scanner.py, tests/test_scanner_semantics.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, src/watchdirs/collect/scanner.py, src/watchdirs/models.py, tests/test_scanner_semantics.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md</read_first>
  <behavior>
    - RED: a directory with direct child directory count `>= collapse.fan_out` collapses with `collapse_reason="fan_out"` per D-04.
    - RED: a narrow deep tree with total descendant directory count `>= collapse.descendants` collapses with `collapse_reason="descendant_count"` per D-05.
    - RED: a deep tree below the descendant threshold does not collapse only because of depth per D-06.
    - RED: `collapsed_dirs` equals the number of folded descendant directories and excludes the collapsed boundary directory itself.
    - RED: a mixed subtree containing ordinary children plus skipped-mount or partial-error descendants collapses without emitting normal descendants, but the collapsed boundary row surfaces folded evidence and `ScanResult.errors` keeps exact evidence paths.
  </behavior>
  <action>Count direct child directories as entries are processed and set the fan-out reason once the count reaches `CollapsePolicy.fan_out`, unless a `known_noise` reason already exists or the path is protected by the exact/descendant/ancestor-poisoning `collapse.never` rules from Task 1. At frame pop, if no earlier reason exists and `frame.dir_count >= CollapsePolicy.descendants`, collapse with `collapse_reason="descendant_count"`. Preserve reason precedence as `known_noise`, then `fan_out`, then `descendant_count`; all three are blocked by protected descendants. Define `collapsed_dirs` exactly as `frame.dir_count`: the count of folded descendant directories, excluding the boundary directory itself, with trigger equality using `>=`. When row-buffer truncation removes descendant rows, do not silently lose evidence: aggregate folded descendant `error` values and skipped-mount evidence into the boundary row's `error` string using a stable prefix such as `collapsed_subtree_evidence`, and keep exact paths in `ScanResult.errors`. Excluded paths stay omitted as before, but skipped mount rows and permission errors under a collapsed boundary must be represented by boundary evidence instead of disappearing. Addresses review concerns A1, A4, and A10.</action>
  <acceptance_criteria>`tests/test_scanner_semantics.py` proves fan-out threshold equality, descendant-count threshold equality, `collapsed_dirs` off-by-one convention, no depth-only collapse, reason precedence, protected-descendant blocking for all triggers, and folded evidence behavior for mixed normal/skipped/error subtrees.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_scanner_semantics.py -x</automated>
  </verify>
  <done>Fan-out and descendant-count safety net collapse reduce emitted rows without changing recursive aggregate totals or filesystem safety semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CollapsePolicy -> scanner traversal | Parsed operator policy controls which filesystem subtrees lose descendant rows. |
| Filesystem entries -> scanner row buffer | Untrusted path names and high-cardinality trees drive scanner memory and emitted rows. |
| Scanner rows -> persistence | Collapse metadata produced here is stored by Plan 01 schema code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03.2-05 | Denial of Service | `scan_root()` missed collapse triggers | mitigate | Tests cover known names, fan-out threshold, and descendant-count safety net; defaults come from `CollapsePolicy`. |
| T-03.2-06 | Tampering | `collapse.never` matching | mitigate | Exact-or-descendant absolute path matching protects allowlisted data paths from collapse. |
| T-03.2-07 | Information Loss | shallowest boundary logic | mitigate | Row-buffer truncation is tested to persist one boundary row, no normal descendants below it, and folded evidence on the boundary row. |
| T-03.2-08 | Tampering | top-child path metadata | mitigate | Scanner stores raw bytes from the direct child row; DB layer binds them through path dictionary resolution. |
| T-03.2-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `uv run --with pytest pytest tests/test_scanner_semantics.py -q`, then run `uv run --with pytest pytest -q -x` before releasing Wave 3.
</verification>

<success_criteria>
- Collapsed rows keep recursive aggregates and folded directory counts.
- Descendant rows below collapsed boundaries are absent from `ScanResult.rows`.
- Protected descendants listed in `collapse.never` prevent ancestor collapse, using path-component-aware matching.
- Folded skipped/error evidence is surfaced on the collapsed boundary and exact evidence paths remain in `ScanResult.errors`.
- `top_child_path` and `top_child_disk_bytes` identify the largest direct child directory by recursive disk bytes.
- Depth alone never triggers collapse.
</success_criteria>

<output>
Create `.planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md` when done.
</output>
