---
phase: 03.2-scan-time-folder-collapse
plan: "01"
subsystem: database
tags: [sqlite, toml, migrations, collapse-policy, tdd]
requires:
  - phase: 03.1-storage-efficiency
    provides: flat path dictionary schema and SCHEMA_VERSION 3 baseline
provides:
  - immutable CollapsePolicy parsing with shipped defaults
  - schema version 4 collapse metadata columns on directory_sizes
  - explicit v3 to v4 SQLite migration with shape verification
affects: [03.2-02, 03.2-03, collect, reporting]
tech-stack:
  added: []
  patterns:
    - explicit additive SQLite migration before user_version advancement
    - immutable top-level collapse policy parsed from TOML
key-files:
  created: []
  modified:
    - src/watchdirs/models.py
    - src/watchdirs/config.py
    - src/watchdirs/db/schema.sql
    - src/watchdirs/db/migrations.py
    - tests/conftest.py
    - tests/test_cli_collect.py
    - tests/test_db_schema.py
    - examples/senbonzakura.watchdirs.toml
key-decisions:
  - "Persist top_child_disk_bytes alongside top_child_id to satisfy the locked D-08 breadcrumb contract."
  - "Migrate only schema version 3 forward to 4; schema versions 1 and 2 fail fast as unsupported pre-dictionary inputs."
  - "Verify directory_sizes collapse column shape before bumping PRAGMA user_version to 4."
patterns-established:
  - "Collapse config rejects unsupported keys and validates absolute never paths through the existing config error envelope."
  - "Schema migrations add missing columns idempotently, then assert the final SQLite table shape."
requirements-completed: [collapse-list, collapse-row-flag, depth-fanout-safety-net]
duration: 7min
completed: 2026-06-17
status: complete
---

# Phase 03.2 Plan 01: Scan-Time Collapse Foundation Summary

**Typed collapse policy parsing and schema version 4 collapse metadata now exist as the durable base for scan-time subtree folding.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-06-16T19:15:33Z
- **Completed:** 2026-06-16T19:22:15Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Added `CollapsePolicy`, `WatchConfig.collapse_policy`, and shipped `[collapse]` defaults with strict validation.
- Extended `directory_sizes` to schema version 4 with persisted collapse metadata and `top_child_disk_bytes`.
- Added an explicit v3 to v4 migration that verifies the final column and foreign-key shape before advancing `user_version`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add typed collapse policy and TOML parsing**
   RED `91a8fd9` (`test`)
   GREEN `9babd6c` (`feat`)
2. **Task 2: Add schema version 4 collapse columns and explicit migration**
   RED `ef683c1` (`test`)
   GREEN `bd36764` (`feat`)

## Files Created/Modified

- `src/watchdirs/models.py` - Added `CollapsePolicy`, collapse metadata defaults on `DirectoryAggregate`, and `ScannerOptions.collapse_policy`.
- `src/watchdirs/config.py` - Added shipped collapse defaults and strict `[collapse]` parsing/validation into `WatchConfig`.
- `src/watchdirs/db/schema.sql` - Added schema version 4 collapse columns to fresh `directory_sizes` tables.
- `src/watchdirs/db/migrations.py` - Added explicit v3 to v4 migration, shape verification, and collapse metadata inserts.
- `tests/conftest.py` - Extended config fixture generation to emit `[collapse]` tables for tests.
- `tests/test_cli_collect.py` - Covered default policy parsing, overrides, malformed config, no-depth contract, and example defaults.
- `tests/test_db_schema.py` - Covered fresh v4 schema, v3 migration, idempotency, partial-state recovery, unsupported legacy schemas, and collapsed row persistence.
- `examples/senbonzakura.watchdirs.toml` - Shipped live `[collapse]` defaults for operators.

## Decisions Made

- Stored `top_child_disk_bytes` inline with `top_child_id` so collapsed rows preserve the path-plus-bytes breadcrumb required by D-08.
- Refused schema versions `1` and `2` during this migration path because only dictionary-shaped version `3` databases are valid upgrade inputs for Phase 03.2.
- Rejected unsupported `[collapse]` keys so `depth` cannot quietly become a storage-time trigger.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification

- `uv run --with pytest pytest tests/test_cli_collect.py -x` -> 33 passed
- `uv run --with pytest pytest tests/test_db_schema.py -x` -> 12 passed
- `uv run --with pytest pytest tests/test_cli_collect.py tests/test_db_schema.py -q` -> 45 passed
- `uv run --with pytest pytest -q -x` -> 206 passed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Scanner-side shallowest-boundary collapse logic can now depend on an immutable `CollapsePolicy` and persisted collapse columns.
- Reporting work can surface collapse metadata and top-child breadcrumbs without another schema change.
- No blockers remain for Plan `03.2-02`.

## Self-Check: PASSED

- FOUND: `src/watchdirs/models.py`
- FOUND: `src/watchdirs/config.py`
- FOUND: `src/watchdirs/db/schema.sql`
- FOUND: `src/watchdirs/db/migrations.py`
- FOUND: `tests/conftest.py`
- FOUND: `tests/test_cli_collect.py`
- FOUND: `tests/test_db_schema.py`
- FOUND: `examples/senbonzakura.watchdirs.toml`
- FOUND: `.planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md`
- FOUND COMMIT: `91a8fd9`
- FOUND COMMIT: `9babd6c`
- FOUND COMMIT: `ef683c1`
- FOUND COMMIT: `bd36764`

---
*Phase: 03.2-scan-time-folder-collapse*
*Completed: 2026-06-17*
