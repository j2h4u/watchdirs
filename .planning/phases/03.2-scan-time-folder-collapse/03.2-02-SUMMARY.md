---
phase: 03.2-scan-time-folder-collapse
plan: "02"
subsystem: collect
tags: [python, scanner, collapse, sqlite, tdd]
requires:
  - phase: 03.2-scan-time-folder-collapse
    provides: collapse policy parsing, schema v4 collapse columns, and persisted top-child metadata
provides:
  - scan-time shallowest-boundary collapse for known-noise, fan-out, and descendant-count triggers
  - protected-descendant blocking and path-component-aware collapse.never matching
  - folded skipped/error evidence summaries and top-child breadcrumbs on collapsed rows
affects: [03.2-03, 03.2-04, collect, reporting]
tech-stack:
  added: []
  patterns:
    - late row-buffer truncation after post-order aggregate collection
    - bounded folded-evidence summaries on collapsed boundary rows
key-files:
  created: []
  modified:
    - src/watchdirs/collect/scanner.py
    - tests/test_scanner_semantics.py
key-decisions:
  - "Collapse stays as a row-emission policy layered on the existing post-order aggregate walk instead of rewriting aggregate math."
  - "collapse.never protection uses exact, descendant, and ancestor-of-never path-component matching over raw filesystem bytes."
  - "Collapsed boundary rows replace descendant error text with a bounded collapsed_subtree_evidence summary while exact descendant paths remain in ScanResult.errors."
patterns-established:
  - "Frames track subtree row_start markers so late descendant_count collapse can truncate only the foldable suffix."
  - "Direct-child top-child breadcrumbs are maintained during _merge_child() using recursive child disk_bytes."
requirements-completed: [collapse-list, collapse-aggregate-preserved, depth-fanout-safety-net, collapse-row-flag]
duration: 6min
completed: 2026-06-17
status: complete
---

# Phase 03.2 Plan 02: Scanner Collapse Semantics Summary

**Scanner-time subtree collapse now preserves recursive aggregates while dropping descendant rows, honoring protected paths, and surfacing bounded folded-evidence summaries on collapsed boundaries.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-06-16T19:28:36Z
- **Completed:** 2026-06-16T19:34:55Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added shallowest-boundary collapse in `scan_root()` for known-noise names, fan-out, and descendant-count triggers.
- Preserved recursive bytes, file counts, descendant counts, and direct-child top-child breadcrumbs on collapsed boundary rows.
- Folded skipped-mount and descendant-error evidence into stable `collapsed_subtree_evidence` summaries without losing exact descendant error paths in `ScanResult.errors`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Collapse known-noise and allowlisted subtrees at the shallowest boundary**
   RED `2734aa1` (`test`)
   GREEN `9fe29bb` (`feat`)
2. **Task 2: Add fan-out and descendant-count safety net semantics**
   RED `07c68c3` (`test`)
   GREEN `9831bdd` (`feat`)

## Files Created/Modified

- `src/watchdirs/collect/scanner.py` - Added collapse trigger evaluation, protected-path matching, row-slice truncation, top-child tracking, and folded-evidence summaries.
- `tests/test_scanner_semantics.py` - Added behavioral coverage for known-noise collapse, shallowest-boundary semantics, protected descendants, fan-out and descendant-count triggers, trigger precedence, and folded-evidence summaries.

## Decisions Made

- Kept `_merge_child()` as the authoritative recursive aggregate path and layered collapse on row persistence only, which preserved existing byte and hardlink semantics.
- Treated `collapse.never` as exact-or-descendant plus ancestor-poisoning matching with path-component boundaries so `/data` never protects `/database`.
- Standardized collapsed boundary evidence as `collapsed_subtree_evidence total=N kinds=...` to keep boundary rows bounded and machine-stable.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Descendant-count collapse always selects the shallowest qualifying boundary, so the equality test had to assert the root-side boundary on a narrow deep tree rather than a lower descendant. The scanner behavior matched the locked D-02 shallowest-boundary rule.

## Verification

- `uv run --with pytest pytest tests/test_scanner_semantics.py -x` -> 24 passed
- `uv run --with pytest pytest tests/test_scanner_semantics.py -q` -> 24 passed
- `uv run --with pytest pytest -q -x` -> 217 passed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Reporting work can now rely on collapsed scanner rows carrying `collapsed`, `collapse_reason`, `collapsed_dirs`, `top_child_path`, `top_child_disk_bytes`, and bounded boundary evidence text.
- Cardinality/size proof work in `03.2-04` can measure real row-count reduction against the now-shipped scanner semantics.
- No blockers remain for `03.2-03`.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md`
- FOUND COMMIT: `2734aa1`
- FOUND COMMIT: `9fe29bb`
- FOUND COMMIT: `07c68c3`
- FOUND COMMIT: `9831bdd`

---
*Phase: 03.2-scan-time-folder-collapse*
*Completed: 2026-06-17*
