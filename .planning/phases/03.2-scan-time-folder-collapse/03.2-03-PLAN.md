---
phase: "03.2-scan-time-folder-collapse"
plan: "03"
type: tdd
wave: 3
depends_on:
  - "03.2-01"
  - "03.2-02"
files_modified:
  - src/watchdirs/models.py
  - src/watchdirs/reporting/queries.py
  - src/watchdirs/reporting/render.py
  - tests/test_reporting_queries.py
autonomous: true
requirements:
  - collapse-row-flag
  - collapse-aggregate-preserved
must_haves:
  truths:
    - "Top, diff, and explain-path rows expose collapsed metadata for collapsed boundary rows per D-09."
    - "Top-child breadcrumb contains path plus disk bytes per D-08."
    - "JSON omits collapse metadata keys for non-collapsed rows while keeping existing report controls unchanged."
    - "Collapsed and expanded indexed boundary rows reconcile to the root recursive aggregate with no double counting, including nested storage-domain boundaries below missing intermediate paths."
    - "Collapsed boundary rows carrying bounded folded evidence participate normally in top/report query rows and storage-domain totals."
    - "Diff metadata uses current-row collapse metadata first and baseline metadata only when the current row is absent."
  artifacts:
    - path: "src/watchdirs/reporting/queries.py"
      provides: "collapse metadata SELECTs and boundary fallback for explain-path"
    - path: "src/watchdirs/reporting/render.py"
      provides: "conditional JSON/text collapse metadata rendering"
    - path: "tests/test_reporting_queries.py"
      provides: "top/diff/explain collapsed metadata tests"
  key_links:
    - from: "src/watchdirs/reporting/queries.py"
      to: "src/watchdirs/db/schema.sql"
      via: "SELECT ds.collapsed/collapse_reason/collapsed_dirs/top_child_id/top_child_disk_bytes"
      pattern: "top_child"
    - from: "src/watchdirs/reporting/queries.py"
      to: "src/watchdirs/reporting/queries.py"
      via: "query_indexed_storage_domain_totals handles collapsed boundary gaps with nearest indexed ancestor logic"
      pattern: "query_indexed_storage_domain_totals"
    - from: "src/watchdirs/reporting/render.py"
      to: "src/watchdirs/models.py"
      via: "TopRow and DiffRow collapse metadata fields"
      pattern: "collapsed"
---

<objective>
Surface collapsed-row metadata in existing reporting query and render paths without adding report-time display controls.

Purpose: Agents must see when a row is an opaque collapsed boundary and have the single top-child breadcrumb needed to decide whether a live re-scan is worth doing.
Output: `TopRow` and `DiffRow` metadata, query joins for `top_child_id`, conditional JSON/text rendering, and tests for top/diff/explain-path behavior.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-VALIDATION.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md
@src/watchdirs/models.py
@src/watchdirs/reporting/queries.py
@src/watchdirs/reporting/render.py
@tests/test_reporting_queries.py
@.planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md
</context>

## Artifacts This Plan Produces

- `src/watchdirs/models.py`: `TopRow` and `DiffRow` collapse metadata fields.
- `src/watchdirs/reporting/queries.py`: collapse metadata SELECTs, storage-domain collapsed-row totals coverage, diff metadata policy-drift semantics, and deepest collapsed-ancestor explain-path lookup.
- `src/watchdirs/reporting/render.py`: conditional JSON/text collapse metadata output.
- `tests/test_reporting_queries.py`: top/diff/storage-domain/explain/render tests for this plan's contracts.

## Review Feedback Incorporated

| Finding | Plan Contract |
| --- | --- |
| H1/M1 | Task 1 requires `query_indexed_storage_domain_totals()` tests and explicitly permits changing its aggregation/subtraction logic until collapsed/expanded boundary rows reconcile to the root recursive aggregate with no double counting. |
| M2 | Task 1 requires folded-evidence collapsed boundary rows to participate normally in top/report query rows and storage-domain totals. |
| A6 | Task 2 documents and tests that `top_child` is a breadcrumb and may reference an omitted or separately collapsed child path without recursive expansion. |
| A7 | Task 2 requires direct deepest-collapsed-ancestor lookup for explain-path, not a display-limited result set. |
| A8 | Task 1 pins current-first, baseline-fallback diff metadata when collapse policy differs between snapshots. |
| A11 | This section lists only Plan 03 artifacts. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Query collapsed metadata for top and diff rows</name>
  <files>src/watchdirs/models.py, src/watchdirs/reporting/queries.py, tests/test_reporting_queries.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md, src/watchdirs/models.py, src/watchdirs/reporting/queries.py, tests/test_reporting_queries.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md</read_first>
  <behavior>
    - RED: `query_top_rows()` returns collapsed metadata and `top_child_path`/`top_child_disk_bytes` for collapsed rows and default false/null metadata for ordinary rows.
    - RED: `query_diff_rows()` carries metadata from the current row when present and from the baseline row for deleted collapsed rows.
    - RED: query paths remain raw bytes through model objects.
    - RED: when baseline and current snapshots use different collapse policies, current collapsed rows expose current metadata, current expanded rows do not inherit stale baseline collapse metadata, and deleted collapsed rows expose baseline metadata.
    - RED: `query_indexed_storage_domain_totals()` reconciles collapsed and expanded indexed boundary rows to the root recursive aggregate with no double counting, including a nested storage-domain boundary below a missing intermediate path.
    - RED: a collapsed boundary row carrying bounded folded evidence from Plan 02 Task 2 still appears in top/report query rows and participates in `query_indexed_storage_domain_totals()` like any other collapsed boundary row.
  </behavior>
  <action>Extend `TopRow` and `DiffRow` in `src/watchdirs/models.py` with `collapsed: bool = False`, `collapse_reason: str | None = None`, `collapsed_dirs: int | None = None`, `top_child_path: bytes | None = None`, and `top_child_disk_bytes: int | None = None`. In `query_top_rows()`, select `ds.collapsed`, `ds.collapse_reason`, `ds.collapsed_dirs`, `tcp.path AS top_child_path`, and `ds.top_child_disk_bytes`, joining `paths tcp` on `tcp.id = ds.top_child_id`. In `query_diff_rows()`, select metadata using the current row first and the previous row only when the current row is absent; join top-child paths for both sides and materialize the same fields into `DiffRow`. This defines policy-drift semantics: current collapsed beats baseline expanded, current expanded clears baseline collapsed metadata for that path, and deleted collapsed rows fall back to baseline metadata. Add `query_indexed_storage_domain_totals()` tests with collapsed boundary rows: the fixture must omit descendants under a collapsed boundary and prove collapsed and expanded indexed boundary rows reconcile to the root recursive aggregate with no double counting, including nested storage-domain boundaries below missing intermediate paths. If those RED tests fail against the existing implementation, modify `query_indexed_storage_domain_totals()` aggregation/subtraction logic itself; nearest-indexed-ancestor subtraction is an implementation candidate, not a constraint that outranks the GREEN invariant. Add a folded-evidence fixture using the Plan 02 bounded marker/count/kind boundary `error` format and prove that row still appears in top/report query rows and contributes normally to storage-domain totals. Keep all path values as bytes and keep grouping behavior unchanged. Addresses review concerns H1, A8, M1, and M2.</action>
  <acceptance_criteria>`tests/test_reporting_queries.py` includes top-row and diff-row tests where collapsed metadata is present, deleted collapsed rows retain baseline metadata, policy-drift cases follow current-first/baseline-fallback semantics, non-collapsed rows keep false/null model values, collapsed/expanded indexed boundary rows reconcile to the root recursive aggregate with no double counting, nested storage-domain boundaries below missing intermediate paths are attributed correctly, and folded-evidence collapsed boundary rows participate normally in top/report rows and storage-domain totals.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_reporting_queries.py -x</automated>
  </verify>
  <done>Top and diff query dataclasses expose collapse metadata from schema version 4 without changing existing grouping, ordering, or classification semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render collapse metadata and explain collapsed boundary paths</name>
  <files>src/watchdirs/reporting/queries.py, src/watchdirs/reporting/render.py, tests/test_reporting_queries.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, src/watchdirs/reporting/queries.py, src/watchdirs/reporting/render.py, tests/test_reporting_queries.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md</read_first>
  <behavior>
    - RED: JSON payloads include `collapsed`, `collapse_reason`, `collapsed_dirs`, and `top_child` only for collapsed rows; ordinary rows do not include those keys.
    - RED: `top_child` JSON is an object with path payload fields plus `disk_bytes`.
    - RED: `query_explain_path_rows()` returns the deepest collapsed indexed ancestor when the requested path is inside a collapsed subtree and no exact descendant row exists.
    - RED: explain-path collapsed-ancestor lookup is not derived from CLI/frontier/deleted limits; it uses a direct deepest-ancestor query.
    - RED: a collapsed row's `top_child` may name a child path that is not emitted under that boundary or that is separately collapsed in another snapshot; renderers show it as a breadcrumb only and do not recursively expand a top-child chain.
  </behavior>
  <action>Add a render helper such as `_collapse_metadata_payload(row)` that returns an empty dict for non-collapsed rows and returns `collapsed: True`, `collapse_reason`, `collapsed_dirs`, and `top_child` for collapsed rows. `top_child` must include the existing `path_payload()` output for `top_child_path` and `disk_bytes: top_child_disk_bytes`; omit `top_child` when there is no direct child directory. Treat `top_child` as a breadcrumb, not a recursive chain: it may point at a child omitted by the collapsed boundary, or at a path that is collapsed in another snapshot, and renderers must not try to expand that chain. Merge this helper into `_top_row_payload()`, `_frontier_row_payload()`, and `_diff_row_payload()`. Add concise text-mode fields only when a row is collapsed, for example `collapsed=true reason=fan_out collapsed_dirs=... top_child=... top_child_disk_bytes=...`; do not add depth caps, row suppression, human-byte formatting, or any new report flag. In `query_explain_path_rows()`, keep exact-match behavior first. If exact target is absent, query candidate ancestors directly from `directory_sizes` for the selected pair and choose the deepest row where `collapsed=1` and the row path is a path-component prefix of `target_path`. Return that boundary row as the target with no fabricated descendants. If no exact or collapsed ancestor exists, keep the existing `path_not_indexed` error. Addresses review concerns A6 and A7.</action>
  <acceptance_criteria>`tests/test_reporting_queries.py` asserts conditional JSON metadata, top-child path plus disk bytes, top-child breadcrumb-only semantics, text output for collapsed rows, no null collapse keys on ordinary rows, direct explain-path collapsed-ancestor lookup, and explain-path returning the collapsed boundary for a path inside a collapsed subtree.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_reporting_queries.py -x</automated>
  </verify>
  <done>Existing reports expose collapse facts and breadcrumbs while preserving JSON-first contracts and avoiding report-time display-control scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| SQLite rows -> reporting dataclasses | Stored collapse metadata becomes agent-facing output. |
| Requested path -> explain-path lookup | User-supplied path may target a descendant that is intentionally not indexed. |
| Raw path bytes -> JSON/text renderers | Filesystem bytes are decoded only at display boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03.2-09 | Information Disclosure | render null metadata | mitigate | Render collapse fields conditionally only for collapsed rows; tests assert ordinary rows omit those keys. |
| T-03.2-10 | Tampering | query top-child joins | mitigate | Join `top_child_id` through `paths` and materialize bytes; no interpolated path SQL. |
| T-03.2-11 | Denial of Service | explain-path inside collapsed subtree | mitigate | Return deepest indexed collapsed ancestor via direct lookup instead of requiring absent descendant rows or scanning limited result sets. |
| T-03.2-12 | Spoofing | collapsed-row agent interpretation | mitigate | Payload includes `collapsed: true`, reason, folded directory count, and top-child bytes so opaque rows are explicit. |
| T-03.2-13 | Tampering | storage-domain totals with collapsed rows | mitigate | Tests prove `query_indexed_storage_domain_totals()` reconciles collapsed boundary rows and absent descendants using nearest indexed ancestor semantics. |
| T-03.2-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `uv run --with pytest pytest tests/test_reporting_queries.py -q`, then run `uv run --with pytest pytest -q -x` before phase verification.
</verification>

<success_criteria>
- Top, diff, report frontier, deleted preview, and explain-path row payloads expose collapse metadata only when present.
- `top_child` includes both path identity and `disk_bytes`.
- Storage-domain totals reconcile collapsed/expanded indexed boundary rows to the root recursive aggregate with no double counting, including nested storage-domain boundaries below missing intermediate paths.
- Collapsed boundary rows carrying bounded folded evidence participate normally in top/report query rows and storage-domain totals.
- Diff metadata has explicit policy-drift semantics across differently collapsed snapshots.
- Explain-path into collapsed internals resolves to the collapsed boundary rather than a fabricated descendant.
- No report-time display controls are added.
</success_criteria>

<output>
Create `.planning/phases/03.2-scan-time-folder-collapse/03.2-03-SUMMARY.md` when done.
</output>
