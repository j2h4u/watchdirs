---
phase: 03.2
slug: scan-time-folder-collapse
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-16
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for scan-time folder collapse.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run --with pytest pytest -q -x` |
| **Full suite command** | `uv run --with pytest pytest -q` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run --with pytest pytest -q -x`
- **After every plan wave:** Run `uv run --with pytest pytest -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.2-01-01 | 01 | 1 | collapse-list | T-03.2-01 | Malformed `[collapse]` config is rejected; `never` paths are normalized and cannot be collapsed | integration | `uv run --with pytest pytest tests/test_cli_collect.py -x` | yes | pending |
| 03.2-01-02 | 01 | 1 | collapse-row-flag | T-03.2-02 | v3 databases gain collapse columns before user_version is advanced; reruns and partial-column states are safe | unit/integration | `uv run --with pytest pytest tests/test_db_schema.py -x` | yes | pending |
| 03.2-02-01 | 02 | 2 | collapse-aggregate-preserved | T-03.2-03 | Collapsed boundary rows preserve recursive apparent bytes, disk bytes, file_count, and folded directory count; protected descendants prevent ancestor collapse | unit | `uv run --with pytest pytest tests/test_scanner_semantics.py -x` | yes | pending |
| 03.2-02-02 | 02 | 2 | depth-fanout-safety-net | T-03.2-04 | Fan-out and descendant-count collapse work; depth alone never triggers collapse; folded skipped/error evidence is surfaced on the boundary | unit | `uv run --with pytest pytest tests/test_scanner_semantics.py -x` | yes | pending |
| 03.2-03-01 | 03 | 3 | collapse-row-flag | T-03.2-09 | Top/diff/storage-domain queries expose collapsed metadata, handle policy drift, and reconcile collapsed storage-domain totals | integration | `uv run --with pytest pytest tests/test_reporting_queries.py -x` | yes | pending |
| 03.2-03-02 | 03 | 3 | collapse-row-flag | T-03.2-11 | Explain-path resolves paths inside collapsed internals to the deepest collapsed boundary through direct lookup and renderers omit null metadata on ordinary rows | integration | `uv run --with pytest pytest tests/test_reporting_queries.py -x` | yes | pending |
| 03.2-04-01 | 04 | 3 | collapse-aggregate-preserved | T-03.2-13 | No-collapse policy preserves pre-collapse scanner/reporting semantics | integration | `uv run --with pytest pytest tests/test_reporting_equivalence.py -x` | yes | pending |
| 03.2-04-02 | 04 | 3 | storage-win proof | T-03.2-15 | Real scan_root output inserted into collapsed vs uncollapsed product-schema DBs reduces row count and VACUUMed per-snapshot bytes | integration | `uv run --with pytest pytest tests/bench/test_size.py -x` | yes | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_scanner_semantics.py` — add known-noise, fan-out, descendant-count, shallowest-boundary, protected-descendant, path-component-aware `never`, folded-evidence, and `collapsed_dirs` fixtures.
- [ ] `tests/test_db_schema.py` — add v3 to v4 migration coverage, idempotent rerun, partial-column state, and defaults for existing rows.
- [ ] `tests/test_reporting_queries.py` — add collapsed metadata assertions for top, diff, storage-domain totals, explain-path, policy-drift metadata, and top-child breadcrumb semantics.
- [ ] `tests/bench/test_size.py` or sibling bench test — add real `scan_root()` to product-schema SQLite row-count / byte-reduction proof, separate from the old blob benchmark.
- [x] `top_child` byte contract is resolved in PLAN.md: persist `top_child_disk_bytes` alongside `top_child_id`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real `/home` cardinality reduction check | storage-win proof | Host-size benchmark may be too expensive for the default test suite | Run the existing size/collection harness against a representative sampled tree and record row-count reduction before verification. |

---

## Validation Sign-Off

- [x] All tasks have automated verify commands or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** draft 2026-06-16
