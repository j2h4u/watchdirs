---
phase: 03.2
slug: scan-time-folder-collapse
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-16
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for scan-time folder collapse.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run --with pytest pytest -q -x` |
| **Full suite command** | `uv run --with pytest pytest -q` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run --with pytest pytest -q -x`
- **After every plan wave:** Run `uv run --with pytest pytest -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.2-01-01 | 01 | 1 | collapse-list | T-03.2-01 | Malformed `[collapse]` config is rejected; `never` paths are normalized and cannot be collapsed | integration | `uv run --with pytest pytest tests/test_cli_collect.py -x` | yes | pending |
| 03.2-01-02 | 01 | 1 | collapse-row-flag | T-03.2-02 | v3 databases gain collapse columns before user_version is advanced | unit/integration | `uv run --with pytest pytest tests/test_db_schema.py -x` | yes | pending |
| 03.2-02-01 | 02 | 2 | collapse-aggregate-preserved | T-03.2-03 | Collapsed boundary rows preserve recursive apparent bytes, disk bytes, file_count, and folded directory count | unit | `uv run --with pytest pytest tests/test_scanner_semantics.py -x` | yes | pending |
| 03.2-02-02 | 02 | 2 | depth-fanout-safety-net | T-03.2-04 | Fan-out and descendant-count collapse work; depth alone never triggers collapse | unit | `uv run --with pytest pytest tests/test_scanner_semantics.py -x` | yes | pending |
| 03.2-03-01 | 03 | 3 | collapse-row-flag | T-03.2-05 | `top`, `diff`, and `explain-path` expose collapsed metadata without leaking null fields | integration | `uv run --with pytest pytest tests/test_reporting_queries.py -x` | yes | pending |
| 03.2-03-02 | 03 | 3 | storage-win proof | T-03.2-06 | Collapse reduces row count / per-snapshot bytes on a fixture or sampled DB | integration | `uv run --with pytest pytest tests/bench/test_size.py -x` | yes | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_scanner_semantics.py` — add known-noise, fan-out, descendant-count, shallowest-boundary, and `never` fixtures.
- [ ] `tests/test_db_schema.py` — add v3 to v4 migration coverage and defaults for existing rows.
- [ ] `tests/test_reporting_queries.py` — add collapsed metadata assertions for top, diff, and explain-path.
- [ ] `tests/bench/test_size.py` or sibling bench test — add row-count / byte-reduction proof.
- [ ] Decide in PLAN.md whether `top_child` persists child bytes as an additional field or explicitly narrows to path-only.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real `/home` cardinality reduction check | storage-win proof | Host-size benchmark may be too expensive for the default test suite | Run the existing size/collection harness against a representative sampled tree and record row-count reduction before verification. |

---

## Validation Sign-Off

- [x] All tasks have automated verify commands or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** draft 2026-06-16
