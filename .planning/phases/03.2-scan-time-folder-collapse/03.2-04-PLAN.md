---
phase: "03.2-scan-time-folder-collapse"
plan: "04"
type: tdd
wave: 3
depends_on:
  - "03.2-01"
  - "03.2-02"
files_modified:
  - src/watchdirs/bench/size.py
  - tests/bench/test_size.py
  - tests/test_reporting_equivalence.py
autonomous: true
requirements:
  - collapse-aggregate-preserved
  - depth-fanout-safety-net
  - collapse-list
must_haves:
  truths:
    - "A no-collapse policy preserves pre-collapse scanner/reporting semantics."
    - "A real `scan_root()` fixture proves collapsed row sets reduce stored row cardinality and measured per-snapshot bytes compared with uncollapsed row sets."
    - "The storage proof compares collapsed versus uncollapsed product-schema SQLite databases, including schema version 4, and does not use the old blob baseline as the collapse proof."
  artifacts:
    - path: "src/watchdirs/bench/size.py"
      provides: "collapse cardinality and size comparison helpers"
    - path: "tests/bench/test_size.py"
      provides: "row-count and per-snapshot byte proof"
    - path: "tests/test_reporting_equivalence.py"
      provides: "no-collapse regression fence"
  key_links:
    - from: "tests/bench/test_size.py"
      to: "src/watchdirs/bench/size.py"
      via: "compare_uncollapsed_vs_collapsed builds product-schema SQLite DBs"
      pattern: "compare_uncollapsed_vs_collapsed"
    - from: "tests/test_reporting_equivalence.py"
      to: "src/watchdirs/collect/scanner.py"
      via: "no-collapse policy keeps Phase 03.1 row semantics"
      pattern: "CollapsePolicy"
---

<objective>
Prove the collapse implementation preserves semantics under an explicit no-collapse policy and reduces cardinality/SQLite size when enabled.

Purpose: Phase 03.2 exists to cut row count and database growth, so the phase needs a storage-facing proof beyond unit-level scanner behavior.
Output: benchmark helpers and tests that compare uncollapsed vs collapsed product-schema row sets using SQLite schema version 4.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-VALIDATION.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-PATTERNS.md
@src/watchdirs/bench/size.py
@tests/bench/test_size.py
@tests/test_reporting_equivalence.py
@tests/test_scanner_semantics.py
@.planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md
@.planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md
</context>

## Artifacts This Plan Produces

- `src/watchdirs/bench/size.py`: `CollapseComparison` and `compare_uncollapsed_vs_collapsed()` for product-schema cardinality and per-snapshot-byte proof.
- `tests/bench/test_size.py`: real `scan_root()` to SQLite collapse-size proof and product-schema-only assertions.
- `tests/test_reporting_equivalence.py`: no-collapse regression fence for expanded directory semantics.

## Review Feedback Incorporated

| Finding | Plan Contract |
| --- | --- |
| A3 | Task 2 requires a synthetic filesystem scanned through real `scan_root()` under collapsed and no-collapse policies before insertion into SQLite. |
| A9 | Task 2 requires collapsed versus uncollapsed product-schema DB comparison and explicitly excludes the old blob baseline from the collapse proof. |
| A11 | This section lists only Plan 04 artifacts. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add no-collapse regression fence for existing reporting semantics</name>
  <files>tests/test_reporting_equivalence.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, tests/test_reporting_equivalence.py, tests/test_scanner_semantics.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md</read_first>
  <behavior>
    - RED: scanning a representative tree with a no-collapse `CollapsePolicy` yields the same row paths, depths, recursive bytes, file counts, and directory counts as the pre-collapse expectations.
    - RED: top/diff golden fixtures built through `_directory_row()` still pass when collapse metadata defaults are false/null.
  </behavior>
  <action>Extend `tests/test_reporting_equivalence.py` with a no-collapse regression test using `CollapsePolicy(names=frozenset(), fan_out=1000000000, descendants=1000000000, never=())` through `ScannerOptions.collapse_policy`. The fixture should include a known-noise basename so the test proves policy control, not accidental absence of triggers. Assert that the no-collapse scan emits all rows and that the existing golden reporting tests continue to operate with metadata defaults. Keep this as a test-only regression fence; do not add any new reporting flags.</action>
  <acceptance_criteria>`tests/test_reporting_equivalence.py` fails before scanner policy support is present and passes when no-collapse policy preserves existing row/report semantics.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/test_reporting_equivalence.py -x</automated>
  </verify>
  <done>No-collapse policy proves backwards-equivalent semantics for tests and fixtures that intentionally need expanded directory rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove collapse cardinality and per-snapshot byte reduction</name>
  <files>src/watchdirs/bench/size.py, tests/bench/test_size.py</files>
  <read_first>.planning/phases/03.2-scan-time-folder-collapse/03.2-CONTEXT.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-RESEARCH.md, src/watchdirs/bench/size.py, tests/bench/test_size.py, .planning/phases/03.2-scan-time-folder-collapse/03.2-01-SUMMARY.md, .planning/phases/03.2-scan-time-folder-collapse/03.2-02-SUMMARY.md</read_first>
  <behavior>
    - RED: a deterministic filesystem fixture scanned through real `scan_root()` under no-collapse and collapsed policies produces equal root recursive aggregates, fewer collapsed rows, and absent descendants below the collapsed boundary.
    - RED: inserting those scanned rows through `initialize_database()`, `create_snapshot()`, and `insert_directory_rows()` produces lower measured product-schema bytes after VACUUM for the collapsed side.
    - RED: the comparison reports both row-count reduction and per-snapshot-byte reduction without calling `compare_old_vs_new()` or using the old blob baseline as the collapse proof.
  </behavior>
  <action>Add `CollapseComparison` and `compare_uncollapsed_vs_collapsed(uncollapsed_rows, collapsed_rows, *, snapshots, churn, workdir)` to `src/watchdirs/bench/size.py`. Build both sides with the product schema using `_build_new_db()` so schema version 4, path dictionary, `top_child_id`, and insert batching are exercised. Return `uncollapsed`, `collapsed`, `uncollapsed_row_count`, `collapsed_row_count`, `row_count_reduction_ratio`, and `per_snapshot_bytes_reduction_ratio`. Keep the existing old-blob dictionary benchmark intact, but do not call it for the collapse proof. In `tests/bench/test_size.py`, create a deterministic filesystem fixture with a known-noise subtree and scan it twice with real `scanner.scan_root()`: once with no-collapse policy and once with the default/name-collapse policy. Assert root recursive apparent bytes, disk bytes, file count, and directory count match; the collapsed scan has fewer rows; descendants below the collapsed boundary are absent; the boundary row has `collapsed=True`, `collapse_reason="known_noise"`, exact `collapsed_dirs`, `top_child_path`, and `top_child_disk_bytes`. Insert both scanned row sets through product SQLite paths and assert collapsed row count is lower, collapsed per-snapshot bytes are lower after VACUUM, both sides report `SCHEMA_VERSION == 4`, no old blob measurement participates in the assertion, and `top_child` storage survives insertion. Addresses review concerns A3 and A9.</action>
  <acceptance_criteria>`tests/bench/test_size.py` proves collapsed product-schema storage from real `scan_root()` output is smaller than uncollapsed product-schema storage for the fixture and reports row-count reduction; existing dictionary benchmark tests still pass separately.</acceptance_criteria>
  <verify>
    <automated>uv run --with pytest pytest tests/bench/test_size.py -x</automated>
  </verify>
  <done>The phase has an automated cardinality and SQLite-size proof that uses direct SQLite initialization/migration, not an ORM push or a report-only row filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Test fixtures -> benchmark helpers | Synthetic row sets represent collapse economics and must not hide storage regressions. |
| Benchmark helpers -> SQLite product schema | Size proof writes real schema version 4 DBs and reads VACUUMed file size. |
| No-collapse policy -> regression harness | Tests intentionally disable collapse and must not mask default collapse behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03.2-13 | Repudiation | storage-size proof | mitigate | Compare real scan-derived uncollapsed and collapsed product-schema DBs after VACUUM and report row counts plus per-snapshot bytes. |
| T-03.2-14 | Tampering | synthetic collapsed rows | mitigate | Fixture asserts recursive aggregate, folded directory count, and top-child bytes before size comparison. |
| T-03.2-15 | Denial of Service | cardinality explosion | mitigate | Automated test fails if collapsed row count is not lower for a known-noise subtree fixture. |
| T-03.2-16 | Tampering | schema drift in benchmark DBs | mitigate | Build both sides through `initialize_database()` and `insert_directory_rows()` with schema version 4. |
| T-03.2-SC | Tampering | package installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run `uv run --with pytest pytest tests/test_reporting_equivalence.py tests/bench/test_size.py -q`, then run `uv run --with pytest pytest -q` as the phase gate after Plan 03 also completes.
</verification>

<success_criteria>
- No-collapse policy preserves existing reporting semantics.
- Collapsed scan-derived row sets demonstrate lower row count and lower VACUUMed per-snapshot bytes than uncollapsed scan-derived row sets.
- Storage proof uses direct SQLite schema initialization and insert paths on product-schema DBs only.
- The full pytest suite is green after all Phase 03.2 plans.
</success_criteria>

<output>
Create `.planning/phases/03.2-scan-time-folder-collapse/03.2-04-SUMMARY.md` when done.
</output>
