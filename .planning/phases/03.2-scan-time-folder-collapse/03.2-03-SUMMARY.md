---
phase: 03.2-scan-time-folder-collapse
plan: "03"
subsystem: reporting
tags: [python, sqlite, reporting, collapse, json, tdd]
requires:
  - phase: 03.2-scan-time-folder-collapse
    provides: collapsed scanner rows, schema v4 collapse columns, and bounded folded-evidence summaries
provides:
  - collapsed metadata on top, diff, report, deleted-preview, and explain-path reporting rows
  - breadcrumb-only top-child rendering with path identity plus disk bytes
  - deepest collapsed-ancestor explain-path fallback for folded subtrees
affects: [03.2-04, report, diff, top, explain-path]
tech-stack:
  added: []
  patterns:
    - current-first and baseline-fallback collapse metadata projection in diff queries
    - shared conditional collapse payload/text rendering across reporting surfaces
key-files:
  created: []
  modified:
    - src/watchdirs/models.py
    - src/watchdirs/reporting/queries.py
    - src/watchdirs/reporting/render.py
    - tests/test_reporting_queries.py
key-decisions:
  - "Diff metadata uses the current row's collapse fields whenever the current row exists, and falls back to baseline metadata only for deleted rows."
  - "Explain-path resolves deep targets inside folded subtrees by directly locating the deepest collapsed indexed ancestor instead of requiring an exact descendant row."
  - "Rendered top_child metadata stays breadcrumb-only: path identity plus disk bytes, with no recursive expansion chain."
patterns-established:
  - "TopRow and DiffRow carry the same collapse metadata shape so query, render, and explain-path code can share helpers."
  - "Conditional collapse rendering omits null collapse keys on ordinary rows while preserving existing JSON-first report contracts."
requirements-completed: [collapse-row-flag, collapse-aggregate-preserved]
duration: 5min
completed: 2026-06-17
status: complete
---

# Phase 03.2 Plan 03: Reporting Collapse Metadata Summary

**Collapsed reporting rows now surface explicit boundary metadata, single-child breadcrumbs, and collapsed-ancestor explain-path fallback without adding new display controls.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-16T19:43:04Z
- **Completed:** 2026-06-16T19:48:11Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Extended reporting row models and SQL projections so top/diff/report payloads can surface collapse flags, reasons, folded directory counts, and top-child breadcrumbs.
- Added current-first/baseline-fallback diff semantics so current expanded rows clear stale baseline collapse metadata while deleted collapsed rows preserve baseline facts.
- Added conditional JSON/text collapse rendering and deepest-collapsed-ancestor explain-path lookup for targets inside folded subtrees.

## Task Commits

Each task was committed atomically:

1. **Task 1: Query collapsed metadata for top and diff rows**
   RED `6a5c4f1` (`test`)
   GREEN `1112828` (`feat`)
2. **Task 2: Render collapse metadata and explain collapsed boundary paths**
   RED `6439eb1` (`test`)
   GREEN `84b4436` (`feat`)

## Files Created/Modified

- `src/watchdirs/models.py` - Added shared collapse metadata fields to `TopRow` and `DiffRow`.
- `src/watchdirs/reporting/queries.py` - Projected collapse columns into top/diff rows and added direct collapsed-ancestor explain-path fallback.
- `src/watchdirs/reporting/render.py` - Added shared conditional collapse payload/text helpers across top, diff, report, deleted, and explain-path rendering.
- `tests/test_reporting_queries.py` - Added RED/GREEN coverage for collapsed top/diff metadata, policy drift, breadcrumb rendering, storage-domain totals, and explain-path folded-subtree behavior.

## Decisions Made

- Kept diff metadata semantics explicit: current rows always win, and baseline collapse metadata is only reused when the current row is absent.
- Treated `top_child` as a breadcrumb contract, not an invitation to recursively expand or synthesize descendant chains in output.
- Resolved explain-path misses inside folded subtrees through a direct collapsed-ancestor query rather than depending on exact descendant persistence.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification

- `uv run --with pytest pytest tests/test_reporting_queries.py -x` -> 22 passed
- `uv run --with pytest pytest tests/test_reporting_queries.py -q` -> 22 passed
- `uv run --with pytest pytest -q -x` -> 222 passed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan `03.2-04` can now measure collapse ROI against stable report/query semantics and no-collapse regression fences.
- Reporting surfaces already expose the collapse metadata and breadcrumb signals needed for the final cardinality/size proof.
- No blockers remain for `03.2-04`.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.planning/phases/03.2-scan-time-folder-collapse/03.2-03-SUMMARY.md`
- FOUND: `src/watchdirs/models.py`
- FOUND: `src/watchdirs/reporting/queries.py`
- FOUND: `src/watchdirs/reporting/render.py`
- FOUND: `tests/test_reporting_queries.py`
- FOUND COMMIT: `6a5c4f1`
- FOUND COMMIT: `1112828`
- FOUND COMMIT: `6439eb1`
- FOUND COMMIT: `84b4436`
