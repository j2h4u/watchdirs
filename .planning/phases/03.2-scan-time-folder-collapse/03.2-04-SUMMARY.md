---
phase: 03.2-scan-time-folder-collapse
plan: "04"
subsystem: bench-and-regression
tags: [python, pytest, sqlite, collapse, tdd]
requires:
  - phase: 03.2-scan-time-folder-collapse
    provides: scanner collapse semantics and reporting collapse metadata
provides:
  - explicit no-collapse regression coverage for expanded scanner/report behavior
  - product-schema collapsed-vs-expanded SQLite size comparison helpers
  - real scan_root storage proof for row-count and VACUUMed byte reduction
affects: [03.2-closeout, collect, reporting, bench]
tech-stack:
  added: []
  patterns:
    - metadata-preserving synthetic snapshot replication for storage benchmarks
    - schema-v4 collapse proof built through initialize_database and insert_directory_rows
key-files:
  created: []
  modified:
    - src/watchdirs/bench/size.py
    - tests/bench/test_size.py
    - tests/test_reporting_equivalence.py
key-decisions:
  - "No-collapse regression coverage should use a known-noise basename under an explicit empty CollapsePolicy so the test proves policy control instead of fixture luck."
  - "Collapsed-vs-uncollapsed storage proof must compare two schema-version-4 product databases rather than reusing the old blob-schema benchmark."
  - "Synthetic benchmark replication must preserve collapsed metadata so persisted top_child and collapsed_dirs survive the proof path."
patterns-established:
  - "Collapsed storage proofs write fixed uncollapsed.sqlite3 and collapsed.sqlite3 scratch databases inside the requested workdir for direct schema inspection."
  - "Bench replication helpers now preserve collapse metadata when cloning paths or snapshot ids."
requirements-completed: [collapse-aggregate-preserved, depth-fanout-safety-net, collapse-list]
duration: 8min
completed: 2026-06-16
status: complete
---

# Phase 03.2 Plan 04: Collapse Proof and No-Collapse Fence Summary

**Phase 03.2 now has both sides of the closeout proof: an explicit no-collapse regression fence and a real schema-v4 SQLite size benchmark showing collapsed scan output stores fewer rows and fewer bytes.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-06-16T19:50:00Z
- **Completed:** 2026-06-16T19:57:53Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added an explicit no-collapse regression fence in `tests/test_reporting_equivalence.py` using a known-noise subtree and an empty `CollapsePolicy`.
- Added `CollapseComparison` and `compare_uncollapsed_vs_collapsed()` in `src/watchdirs/bench/size.py` to build schema-v4 product databases for expanded and collapsed row sets.
- Added a real `scan_root()`-driven size proof in `tests/bench/test_size.py` that verifies lower collapsed row count, lower VACUUMed SQLite size, schema version `4`, and persisted `top_child` breadcrumb storage.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add no-collapse regression fence for existing reporting semantics**
   `3d72d91` (`test`)
2. **Task 2: Prove collapse cardinality and per-snapshot byte reduction**
   RED `54dbccb` (`test`)
   GREEN `d6aabf7` (`feat`)

## Files Created/Modified

- `tests/test_reporting_equivalence.py` - Added no-collapse scanner equivalence coverage and explicit false/null collapse-metadata assertions for golden reporting fixtures.
- `tests/bench/test_size.py` - Added a real scanned known-noise fixture and schema-v4 collapsed-vs-uncollapsed SQLite proof assertions.
- `src/watchdirs/bench/size.py` - Added `CollapseComparison`, `compare_uncollapsed_vs_collapsed()`, and metadata-preserving benchmark replication.

## Decisions Made

- Kept the no-collapse proof policy explicit (`names=frozenset(), fan_out=1_000_000_000, descendants=1_000_000_000`) so the regression test demonstrates disabled collapse rather than relying on a benign fixture.
- Measured collapsed-vs-uncollapsed storage through the existing product-schema write path only; the legacy blob benchmark remains intact but is not part of the collapse ROI proof.
- Preserved `collapsed`, `collapse_reason`, `collapsed_dirs`, and `top_child_*` during synthetic snapshot replication so the proof validates persisted schema behavior instead of only row counts.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first size-proof fixture was too small to change SQLite page counts, so both databases rounded to the same `page_count * page_size` even though row counts differed. The fixture was expanded until the proof measured a real persisted byte delta instead of a sub-page difference.

## Verification

- `uv run --with pytest pytest tests/test_reporting_equivalence.py -x` -> 6 passed
- `uv run --with pytest pytest tests/bench/test_size.py -x` -> 5 passed
- `uv run --with pytest pytest tests/test_reporting_equivalence.py tests/bench/test_size.py -q` -> 11 passed
- `uv run --with pytest pytest -q` -> 225 passed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase `03.2` now has the required storage-facing proof for collapse ROI and the regression fence for expanded reporting semantics.
- No blockers remain inside `03.2`; the phase is ready to close and hand off to Phase 4 scheduling/retention work.

## TDD Gate Compliance

- Plan-level TDD gates are present: `test` commits `3d72d91` and `54dbccb`, followed by `feat` commit `d6aabf7`.
- Task 1's RED-phase assertions passed immediately because Plans `03.2-02` and `03.2-03` had already delivered the no-collapse behavior; this plan's contribution there was the explicit regression fence.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.planning/phases/03.2-scan-time-folder-collapse/03.2-04-SUMMARY.md`
- FOUND: `src/watchdirs/bench/size.py`
- FOUND: `tests/bench/test_size.py`
- FOUND: `tests/test_reporting_equivalence.py`
- FOUND COMMIT: `3d72d91`
- FOUND COMMIT: `54dbccb`
- FOUND COMMIT: `d6aabf7`
