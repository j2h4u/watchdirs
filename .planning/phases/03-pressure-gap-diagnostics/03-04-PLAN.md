---
phase: 03-pressure-gap-diagnostics
plan: 04
type: tdd
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/watchdirs/cli.py
  - src/watchdirs/models.py
  - src/watchdirs/diagnostics/__init__.py
  - src/watchdirs/diagnostics/df_index.py
  - src/watchdirs/diagnostics/summary.py
  - src/watchdirs/reporting/__init__.py
  - src/watchdirs/reporting/queries.py
  - src/watchdirs/reporting/render.py
  - tests/test_diagnostics_summary.py
  - tests/test_cli_report_commands.py
autonomous: true
requirements:
  - DIAG-03
  - DIAG-05
user_setup: []
must_haves:
  truths:
    - "`watchdirs report --since 24h --json` includes bounded diagnostic hints when df/index mismatch thresholds fire."
    - "Report-time df/index hints use the revised non-overlapping indexed totals and call `statvfs()` only for indexed storage-domains."
    - "Report-time stale or absent mountpoint stat failures become `filesystem_stat_unavailable` hints/warnings for the affected storage-domain, not report crashes."
    - "Partial filesystem coverage produces partial-scope hints instead of false deleted-open hints."
    - "Partial snapshot evidence is emitted in report hints and prevents deleted-open suspicion from df/index remainder arithmetic unless independent deleted-open evidence exists."
    - "Final diagnostic summaries use compact top-N sections, truncation metadata, and prioritized next checks per D-14 through D-16."
    - "Capacity guidance remains cautious evidence for upgrade, migration, or repurposing decisions; it never claims cleanup or hardware action is safe per D-17."
    - "Deleted-open and Docker evidence are referenced as next checks or compact summaries, not persisted directory attribution."
  artifacts:
    - path: "src/watchdirs/diagnostics/summary.py"
      provides: "compact pressure summary and next-check prioritization"
      exports: ["build_compact_pressure_summary"]
    - path: "src/watchdirs/reporting/render.py"
      provides: "report diagnostic_hints and pressure_summary render fields"
      exports: ["render_report_payload"]
    - path: "tests/test_diagnostics_summary.py"
      provides: "DIAG-03 and DIAG-05 summary/truncation tests"
  key_links:
    - from: "src/watchdirs/cli.py"
      to: "src/watchdirs/diagnostics/df_index.py"
      via: "report computes cheap df/index hint for indexed storage-domains without broad rescan"
      pattern: "build_df_index_diagnostic"
    - from: "src/watchdirs/diagnostics/summary.py"
      to: "src/watchdirs/reporting/render.py"
      via: "compact summary becomes JSON/text output"
      pattern: "pressure_summary"
    - from: "src/watchdirs/reporting/queries.py"
      to: "src/watchdirs/reporting/render.py"
      via: "storage-domain growth and mismatch evidence become report hints"
      pattern: "diagnostic_hints"
---

## Phase Goal

**As a** disk-pressure investigation agent, **I want to** get a short prioritized "where did free space go?" diagnostic, **so that** I can decide the next verification checks without scanning a huge report.

<objective>
Integrate compact Phase 3 diagnostic hints into `report` and finish DIAG-03 and DIAG-05.

Purpose: Convert the df/index, deleted-open, and Docker slices into a bounded final answer while honoring D-14, D-15, D-16, and D-17.
Output: Summary module, report diagnostic hints, pressure summary render fields, CLI integration, phase verification tests.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
@/home/j2h4u/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@AGENTS.md
@README.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pressure-gap-diagnostics/03-CONTEXT.md
@.planning/phases/03-pressure-gap-diagnostics/03-RESEARCH.md
@.planning/phases/03-pressure-gap-diagnostics/03-PATTERNS.md
@.planning/phases/03-pressure-gap-diagnostics/03-01-SUMMARY.md
@.planning/phases/03-pressure-gap-diagnostics/03-02-SUMMARY.md
@.planning/phases/03-pressure-gap-diagnostics/03-03-SUMMARY.md
@src/watchdirs/cli.py
@src/watchdirs/diagnostics/df_index.py
@src/watchdirs/diagnostics/deleted_open.py
@src/watchdirs/diagnostics/docker.py
@src/watchdirs/reporting/queries.py
@src/watchdirs/reporting/render.py
@tests/test_cli_report_commands.py
</context>

## Review Feedback Handling

Cycle 1 and Cycle 2 review findings from `.planning/phases/03-pressure-gap-diagnostics/03-REVIEWS.md` are resolved in this plan:

| Finding | Severity | Resolution in this plan |
|---|---:|---|
| Report hints inherited false df/index deleted-open suspicion | HIGH | Task 1 and Task 2 require report hints to consume `filesystem_scope_extends_beyond_indexed_roots` and avoid `deleted_open_file_suspected` when the remainder is explained by partial filesystem coverage. |
| Report-time `statvfs()` should be scoped to indexed storage domains | LOW | Task 1 and Task 2 require report integration to call stat providers only for domains returned by `query_indexed_storage_domain_totals()`. |
| Containerd warning should remain visible in final summaries | MEDIUM | Task 1 and Task 2 require summary handling for `containerd_enrichment_unavailable` / `containerd_available=false` without containerd category totals. |
| Preserve compact output and cautious next checks | Existing constraint | D-14 through D-17 tests remain required, with top-N caps and verification-only next checks. |
| Per-domain `statvfs()` failure can abort report hints | MEDIUM | Cycle 2 resolved: Task 1 and Task 2 require report-time df/index hints to surface `filesystem_stat_unavailable` warnings/hints for stale or absent mountpoints and continue rendering the report. |
| Partial snapshot status can weaken deleted-open conclusions | LOW | Cycle 2 resolved: tests require partial snapshot evidence in report hints and prevent deleted-open suspicion from df/index remainder arithmetic when selected snapshot evidence is non-complete unless independent deleted-open evidence exists. |

## Artifacts This Phase Produces

- `src/watchdirs/diagnostics/summary.py`
- `src/watchdirs/models.py` dataclasses: `PressureSummarySection`, `PressureSummary`; summary inputs consume df/index fields including `filesystem_scope_extends_beyond_indexed_roots`, `filesystem_stat_available`, `filesystem_status`, `coverage_reason_codes`, partial snapshot evidence, and `over_indexed_bytes`
- `src/watchdirs/reporting/render.py` `diagnostic_hints` and `pressure_summary` report fields
- `src/watchdirs/cli.py` bounded report integration
- `tests/test_diagnostics_summary.py`
- `tests/test_cli_report_commands.py` report hint coverage

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED compact summary and report-hint contract</name>
  <files>tests/test_diagnostics_summary.py, tests/test_cli_report_commands.py</files>
  <behavior>
    - Test 1: df/index material mismatch with full filesystem coverage produces `deleted_open_file_suspected` and `unattributed_usage` report hints.
    - Test 2: df/index material mismatch with `filesystem_scope_extends_beyond_indexed_roots=true` produces a partial-scope/unindexed-filesystem-coverage hint and does not produce `deleted_open_file_suspected`.
    - Test 3: df/index material mismatch with non-complete selected snapshot evidence produces `partial_snapshot_evidence` and does not produce `deleted_open_file_suspected` from the remainder alone.
    - Test 4: report-time df/index reconciliation calls statvfs only for storage domains returned by the indexed-total query.
    - Test 5: report-time statvfs `OSError` for one stale or absent mountpoint emits `filesystem_stat_unavailable` warning/hint and the report still includes other diagnostic hints.
    - Test 6: storage-domain pressure summary ranks top-N domains by unattributed bytes, over-indexed bytes, usage ratio, and recent growth evidence.
    - Test 7: Docker evidence adds category context only when available; containerd path hints add `containerd_enrichment_unavailable` context without category totals.
    - Test 8: summary output includes `limits`, per-section `truncated`, and global `truncated_sections`.
    - Test 9: next checks are prioritized and verification-only, capped to a small fixed count.
    - Test 10: full suite still passes and final commands cover DIAG-01 through DIAG-05.
  </behavior>
  <action>Create failing tests for the compact final diagnostic. Use synthetic df/index sections, deleted-open totals, Docker category records, containerd path-hint records, and existing report group summaries to avoid host-dependent assertions. Assert D-14 and D-15: the summary has no more than 4 sections by default and no more than 5 facts or next checks per section. Assert D-16: every capped array has truncation metadata and the envelope has `limits` plus `truncated_sections`. Assert D-17: text and JSON use cautious labels such as "evidence", "likely reason", and "next check"; they must not say an upgrade, data move, service action, or cleanup is safe. For DIAG-03, extend `tests/test_cli_report_commands.py` so `report --since 24h --json` contains a bounded `diagnostic_hints` array when an injected df/index mismatch exceeds the thresholds from 03-01. Add report-hint cases: full filesystem coverage with complete snapshot evidence may emit `deleted_open_file_suspected` plus `unattributed_usage`; `filesystem_scope_extends_beyond_indexed_roots=true` must emit a partial-scope hint such as `filesystem_scope_extends_beyond_indexed_roots` and must not emit `deleted_open_file_suspected` from that remainder alone; non-complete selected snapshot evidence or partial scan counters must emit `partial_snapshot_evidence` and must not emit deleted-open suspicion from df/index remainder arithmetic without independent deleted-open evidence. Assert report integration uses the 03-01 df/index builder or query in a way that calls statvfs only for indexed storage-domains. Add an injected report-time stat provider case that raises `OSError` for one storage-domain path and succeeds for another; assert `report --since 24h --json` still succeeds, includes a `filesystem_stat_unavailable` warning or diagnostic hint for the stale/absent mountpoint, marks that domain unavailable, and continues rendering other domains and report sections. The hint must point to `watchdirs deleted-open-files --json` and `watchdirs df-vs-index --json`, not inline full live probe dumps. For DIAG-05, assert storage-domain summaries include filesystem usage ratio when stat data is available, indexed visible bytes, `over_indexed_bytes` when present, recent growth bytes from report grouping, Docker reclaimable category evidence when supplied, and containerd-unavailable path-hint context when supplied. Prioritized capacity next checks may mention upgrade, migration, or repurposing evaluation without making the decision.</action>
  <verify>
    <automated>python3 -m pytest -q tests/test_diagnostics_summary.py</automated>
    <automated>python3 -m pytest -q tests/test_cli_report_commands.py -k diagnostic</automated>
  </verify>
  <acceptance_criteria>
    - RED run fails because compact summary/report hint integration does not exist.
    - Tests prove D-14 through D-17 compactness, truncation, and cautious tone.
    - Tests prove partial filesystem coverage does not create deleted-open suspicion.
    - Tests prove report-time statvfs calls are limited to indexed storage-domains.
    - Tests prove report-time statvfs failures produce `filesystem_stat_unavailable` hint/warning output and do not crash the report.
    - Tests prove partial snapshot evidence blocks deleted-open suspicion from df/index arithmetic alone.
    - Tests prove DIAG-03 report suspicion and DIAG-05 storage-domain pressure summary.
  </acceptance_criteria>
  <done>Failing tests define the final Phase 3 compact diagnostic output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN report diagnostic hints and final pressure summary</name>
  <files>src/watchdirs/cli.py, src/watchdirs/models.py, src/watchdirs/diagnostics/__init__.py, src/watchdirs/diagnostics/df_index.py, src/watchdirs/diagnostics/summary.py, src/watchdirs/reporting/__init__.py, src/watchdirs/reporting/queries.py, src/watchdirs/reporting/render.py</files>
  <behavior>
    - `build_compact_pressure_summary()` combines df/index sections, report growth groups, deleted-open totals, Docker enrichment, and containerd path-hint warnings into bounded sections.
    - `report --json` includes `diagnostic_hints` when df/index mismatch thresholds fire.
    - The report hint path runs only cheap statvfs/index reconciliation for indexed storage-domains and does not run lsof or Docker probes automatically.
    - Final output includes prioritized verification-only next checks and truncation metadata.
  </behavior>
  <action>Implement `src/watchdirs/diagnostics/summary.py` as pure transformation logic over dataclasses from previous plans. Keep it deterministic, side-effect free, and unit-testable. Extend `ReportSummary` or add a sibling report diagnostic dataclass so `render_report_payload()` can include `diagnostic_hints` and `pressure_summary` without changing existing Phase 2 field meanings. In `run_report()`, compute a cheap df/index diagnostic using persisted indexed totals plus `os.statvfs()` for the indexed storage-domains returned by `query_indexed_storage_domain_totals()` only; do not enumerate every live mount and do not run deleted-open or Docker probes during report. Inherit the 03-01 per-domain stat failure behavior: if the df/index builder marks a stale or absent mountpoint with `filesystem_stat_available=false` and `filesystem_stat_unavailable`, report output must render a warning or diagnostic hint for that storage-domain and keep the rest of the report successful. When a material unattributed remainder exists, add a compact hint with facts, likely reasons, and next checks per D-05 through D-07. If the df/index section has `filesystem_scope_extends_beyond_indexed_roots=true`, the hint must identify partial filesystem coverage or unindexed filesystem scope and must not emit `deleted_open_file_suspected` from that remainder alone. If the df/index section has non-complete selected snapshot evidence or partial scan/skipped subtree counters, emit `partial_snapshot_evidence` and block deleted-open suspicion from df/index arithmetic alone. Deleted-open suspicion requires full filesystem coverage plus complete snapshot evidence in the df/index section or independent deleted-open evidence supplied to the summary builder. The full deleted-open and Docker evidence stays behind explicit commands from 03-02 and 03-03. Consume 03-03 containerd fields honestly: include `containerd_available=false`, `containerd_path_hints`, and `containerd_enrichment_unavailable` warning context, but do not create containerd category totals. Implement top-N pressure summary sections with defaults `max_sections=4` and `max_items_per_section=5`; include truncation flags at every cap. Rank sections by material unattributed bytes first, then `over_indexed_bytes` snapshot-skew evidence, then high filesystem usage ratio when stat data is available, then recent storage-domain growth, then Docker reclaimable category evidence. DIAG-05 output should support capacity decisions by presenting evidence and next checks for upgrade, migration, or older-disk repurposing evaluation, but it must not prescribe those actions. Keep JSON stable and text terse. Keep scope to the Phase 3 diagnostic contract and do not add broad live filesystem rescans.</action>
  <verify>
    <automated>python3 -m pytest -q tests/test_diagnostics_summary.py</automated>
    <automated>python3 -m pytest -q tests/test_cli_report_commands.py tests/test_diagnostics_df_index.py tests/test_diagnostics_deleted_open.py tests/test_diagnostics_docker.py</automated>
    <automated>python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `watchdirs report --since 24h --json` contains bounded diagnostic hints when mismatch thresholds fire.
    - Report hints do not emit deleted-open suspicion for partial filesystem coverage.
    - Report-time statvfs probes are limited to indexed storage-domains.
    - Report-time statvfs failures become `filesystem_stat_unavailable` hints/warnings and do not crash `report`.
    - Partial snapshot evidence is visible and gates deleted-open suspicion from df/index arithmetic alone.
    - Pressure summary sections are top-N, truncated, and prioritized.
    - Final output contains verification-only next checks and cautious capacity wording.
    - DIAG-01 through DIAG-05 all have automated coverage.
  </acceptance_criteria>
  <done>DIAG-03 and DIAG-05 are complete, and the Phase 3 diagnostic output answers the short wtf/free-space-loss job.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 3 final verification and review feedback closeout</name>
  <files>.planning/phases/03-pressure-gap-diagnostics/03-04-SUMMARY.md</files>
  <action>Run the full diagnostics and regression suite. If `.planning/phases/03-pressure-gap-diagnostics/03-REVIEWS.md` exists, confirm every Cycle 1 HIGH/actionable item is implemented in code/tests or explicitly rejected in the SUMMARY with the locked decision it conflicts with. Record final DIAG-01 through DIAG-05 command evidence, including `watchdirs df-vs-index --json`, `watchdirs deleted-open-files --json`, `watchdirs docker-enrichment --json`, and `watchdirs report --since 24h --json` using deterministic fixtures or safe live no-mutation probes. The closeout must specifically mention non-overlapping df/index aggregation, partial filesystem coverage gating, indexed-only statvfs calls, `over_indexed_bytes`, injected deleted-open probes, containerd unavailable warnings, and report hint gating. Do not run cleanup, service-control, or Docker mutation commands.</action>
  <verify>
    <automated>python3 -m pytest -q tests/test_diagnostics_df_index.py tests/test_diagnostics_deleted_open.py tests/test_diagnostics_docker.py tests/test_diagnostics_summary.py</automated>
    <automated>python3 -m pytest -q</automated>
  </verify>
  <done>Full suite is green, review feedback is incorporated or explicitly resolved, and the SUMMARY records DIAG-01 through DIAG-05 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted report evidence to summary | Summary combines recursive aggregates and live control totals without creating new attribution. |
| Report command to live filesystem stats | Report may run cheap statvfs/index reconciliation but not broad rescans or heavy probes. |
| Diagnostic wording to operator action | Output can influence cleanup/capacity decisions but must remain evidence and next checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Repudiation | compact pressure summary | mitigate | Include source sections, snapshot ids, truncation fields, and warning propagation. |
| T-03-17 | Tampering | report diagnostic_hints | mitigate | Hints derive from tested df/index thresholds; no fabricated directory attribution. |
| T-03-18 | Denial of Service | report-time diagnostics | mitigate | Report runs only cheap statvfs/index reconciliation for indexed storage-domains; lsof and Docker remain explicit commands. |
| T-03-19 | Spoofing | text recommendations | mitigate | Use escaped text fields and cautious evidence labels. |
| T-03-20 | Tampering | operational advice | mitigate | Verification-only next checks; no destructive cleanup, service-control, or Docker mutation commands. |
| T-03-21 | Repudiation | partial filesystem coverage | mitigate | Carry `filesystem_scope_extends_beyond_indexed_roots` into hints and block deleted-open suspicion when the indexed roots cover only part of the filesystem. |
| T-03-22 | Repudiation | report-time unavailable filesystem stats | mitigate | Render `filesystem_stat_unavailable` warning/hint evidence and continue the report when one indexed storage-domain cannot be statted. |
| T-03-23 | Repudiation | partial snapshot evidence | mitigate | Carry partial snapshot counters/reason codes into hints and block deleted-open suspicion when selected snapshot evidence is non-complete. |
| T-03-SC | Tampering | package installs | mitigate | No package-manager install task exists; no dependencies added. |
</threat_model>

<verification>
Run all diagnostics tests, then `python3 -m pytest -q`. Confirm report hints and pressure summary remain compact and non-mutating.
</verification>

<success_criteria>
- DIAG-03 is satisfied by bounded report diagnostic hints.
- DIAG-05 is satisfied by compact storage-domain pressure summaries.
- Report hints consume revised df/index semantics, including partial filesystem coverage and `over_indexed_bytes`.
- Report hints inherit per-domain `statvfs()` failure handling and partial snapshot evidence gating.
- D-14 through D-17 are enforced by tests and output shape.
- Final commands cover DIAG-01 through DIAG-05 without cleanup or service-control behavior.
</success_criteria>

<output>
Create `.planning/phases/03-pressure-gap-diagnostics/03-04-SUMMARY.md` when done.
</output>
